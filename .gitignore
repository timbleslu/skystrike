.DS_Store
.claude/settings.local.json
node_modules/
www/
.vercel
*.png

# iOS Capacitor scaffold — removed from the repo; regenerate locally with `npx cap add ios`
ios/
capacitor.config.json

# graphify knowledge graph (derived artifact — rebuilt locally by post-commit/post-checkout hooks)
graphify-out/

# claude-mem auto-sidecars (machine-local mem logs, not source) — keep only root CLAUDE.md
**/CLAUDE.md
!/CLAUDE.md

# local subagent worktrees (never commit)
.claude/worktrees/
