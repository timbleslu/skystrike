# SKYSTRIKE // ACE PROTOCOL

Arcade jet combat game. Three.js (r159, vendored), single HTML page, no build step, no framework, no modules.

**Source of truth: GitHub `timbleslu/skystrike`, worked via this clone. `git fetch`/`pull` before editing. Ignore any Obsidian Vault or `~/Downloads` copy of Skystrike — they are stale decoys, never source.**

## Run / Test
- Play: open `index.html` in a browser (or any static server).
- Mobile motion test: `npm run serve:https` — LAN HTTPS static server (self-signed cert; accept the warning on the phone). DeviceOrientation needs a secure context, so `file://` / plain http yields zero events.
- Tests: `npm test` — plain Node scripts in `tests/*.test.js`, no test framework. Pure cores live in `js/core.js` and are `require()`d directly by tests (one source of truth — no byte-identical mirror copies). A few legacy tests still mirror trivial one-liners (`disposeGroup`/`cacheGeo`/`wingShape`) or `readFileSync`-assert an architectural invariant (`tests/storage.test.js` enforcing the no-localStorage-outside-storage.js rule — the scrape is the point); new pure logic should go in `core.js` (or another require-safe file) and be imported, NOT mirrored.
- Visual check: `node scripts/shot.mjs <outPrefix> [tod 0|1|2]` — boots the game headless (playwright devDep) and saves hangar/flight/fx/terrain screenshots. Use this to verify any graphics change.
- Close-up check: `node scripts/beauty.mjs <outPrefix> [jetIndex] [tod]` — UI-hidden hangar close-ups (jet front/side/rear + missile/tracer ordnance board). Use when judging model/material quality.

## Architecture (read this, skip exploration)
All code is browser globals — no imports/exports. Script load order in `index.html` defines availability:
`vendor/three.min.js → vendor/GLTFLoader.js → storage.js → core.js → roster.js → globals.js → i18n.js → engine.js → airframes.js → entities.js → rival.js → meta.js → opmap.js → missions.js → combat.js → hud.js → ui-hud.js → ui-tech.js → ui-hangar.js → ui-flow.js → ui-settings.js → controls.js → main.js`

**Pure-core seam (`js/core.js`):** all dependency-free game logic lives here, loaded FIRST. In the browser it defines plain globals like every other file; in Node it `module.exports`-es them (footer guarded by `typeof module`, inert in the browser) so `tests/*.test.js` `require('../js/core.js')` and exercise the REAL implementation. This replaced the old pattern where each test carried a byte-identical copy guarded by a string-equality assert. **Keep `core.js` pure — no THREE/store/DOM references, or it stops being require-safe.**

| File | Role |
|---|---|
| `js/core.js` | **Pure, dependency-free game cores — require-safe (loaded 1st).** Math helpers (`clamp`/`lerp`/`damp`/`rand`/`randInt`/`TWO_PI`/`DEG`); weather (`WEATHER`/`NIGHT_RADAR_MUL` + pure `resolveWeather`/`turbSample`/`rollWeather`, `tests/weather.test.js`); boss-phase (`BOSS_PHASE2_HP`=0.6/`BOSS_PHASE3_HP`=0.3 + `bossPhaseFor`→1/2/3 + monotonic `nextBossPhase`, `tests/boss-phase.test.js`); boss-rush (`BOSS_RUSH_POOL`/`BOSS_RUSH_TOTAL` + `bossRushNext`/`bossRushDone`/`betterTime`, `tests/meta.test.js`); tutorial (`TUTORIAL_STEPS`/`TUTORIAL_DONE`/`TUTORIAL_EVENT_FOR_STEP` + `tutorialNext`, `tests/tutorial.test.js`); daily (`makeRng` mulberry32 + `dailySeedFor` — never reads the clock, `tests/daily.test.js`); camera-shake (`CAMSHAKE_RATE`/`CAMSHAKE_K` + `decayShake`, `tests/camshake.test.js`); AWACS — **COOLDOWN-GATED, not RP-costed (balance 2026-06)** (`AWACS_COOLDOWNS`{strike:30,resupply:26,jam:18}/`AWACS_USES_MAX`{1,1,2}/`AWACS_JAM_TIME`=8 + pure `awacsCall(state{uses,last},cd,max,key,now)`→`{ok,reason,uses,last}` + pure adapter-decision `awacsResolve(...)`→`{ok,reason,uses,last,effect,banner}`; failure reasons `cooldown`/`empty`/`unknown`, `tests/awacs.test.js` + `tests/awacs-adapter.test.js`); cadence/density (balance 2026-06: `shouldOpenTechScreen(wave,wasBoss)` tech-shop cadence; `nextBossOffset`/`isBossWave`/`BOSS_WINDOW_MIN`=3/`BOSS_WINDOW_MAX`=5 windowed boss schedule; `waveCount`/`WAVE_COUNT_CAP`=16 density; `isWildcardWave` rare non-boss spike, `tests/cadence.test.js`); barrel-roll (`rollDetect`/`rollCooldownGate`, `tests/barrel-roll.test.js`); steering (`STEER` tunables incl. `autoMaxBank`≈29°/`autoYawGain` + pure `steerCommand(scheme,intent,currentBank,t)`: 'auto'=gentle bank-hold capped at autoMaxBank w/ heading turned by a WORLD-yaw in combat.js so pitch stays free; 'pointer'=bank-hold/auto-level; 'rate'=legacy roll-rate, `tests/steer.test.js`); aim-assist (`AIM_ASSIST_LEVELS` 5-step strength table — `aimStrength` slider sets the DETECTION FIELD (`cone`/`range`) AND a proportional MAX force (`maxRate`) — + `AIM_ASSIST` level-3 alias + `AIM_MAGNET_K`=18 + pure `aimAssistCfg(level,manual)`→preset and `aimAssistStep(angErr,dist,dt,cfg)`→rate-capped radians toward the gun lead point as an inverse-square MAGNET: `pullForce = maxForce/(1+AIM_MAGNET_K·angErr²)` — MAX force on the lead pip (angErr≈0 ≈100%, 15°≈45%, 30°≈17%, 45°≈8%), inert outside the cone/range field, still eased out with distance + clamped so it never overshoots; the top tier yields to manual input, `tests/aim-assist.test.js`); gfx-quality (`GFX_TIERS`=['auto','low','medium','high'] + pure `resolveQuality(setting,dpr,isTouch)` — explicit low/medium/high pass through, auto→**medium** for ANY touch device else high, `tests/gfx-quality.test.js`); **environment tiers (Track B terrain/sea/fog/ground): pure `TERRAIN_TIER`/`SEA_TIER` per-tier SEG+feature tables, `terrainDetailH(x,z,cfg)` (tier-only VISUAL relief layered on terrainH, 0 on Low, bounded ±detailAmp), `fogDensityFor(tier,type)` (`FOG_CLEAR_DENSITY` clear baseline 28/34/38km + `FOG_ACTIVE_DENSITY` fixed storm~6km/fog~3km — WEATHER.storm/fog fogMul raised 1.6→5.7 / 3.0→11.4), `GROUNDOBJ_TIER` spawn caps + `planGroundObjects(seed,tier,terrainHFn)` (deterministic InstancedMesh placement: water/slope/platform/radial-falloff rejection, terrainHFn injected so core stays THREE-free), `tests/terrain-tier.test.js`;** instrument seam (pure `instrumentState(kt,altFt,throttle)`→`{spdFrac,altFrac,thrFrac,spdDeg,thrDeg,altDeg,altDegK,spdKt,altFt}` — full-scale 1000kt/20000ft, pre-clamped needle angles consumed by the per-skin HUD gauges, `tests/instruments.test.js`); control input shaping (`shapeAxis`/`mapFlightInput`/`motionAxis`/`emaSmooth` + `AGGRESSION` presets, `tests/controls.test.js`); **campaign cores (Operations revamp): `LEVEL_WAVE_CAP`=4 + `campaignWaveCount(index,cap)` bounded wave-bank; `levelCleared`; progression `isOpUnlocked`/`isLevelUnlocked`/`levelState`/`markLevelCleared`/`furthestLevel` (PURE over `(campaign, OPERATIONS, …)` — OPERATIONS injected so core stays load-order-free); checkpoint `captureSnapshot`/`rollbackSnapshot`; `grantLevelRewards`/`CAMPAIGN_REPLAY_REWARDS`; bounded clear-target `campaignClearTarget(verb,wave,spawn)`=`min(procedural, spawnedKillCount)` so a bounded wave's kill target never exceeds the kill-targets it actually spawns (`null`=non-kill verb; single-phase path in missions.js clamps `mission.target` through it; endless mode unchanged), `tests/clear-count.test.js`, `tests/campaign.test.js`**. **Lock-on / targeting (Candidate B consolidation): pure `advanceLock(lock{progress,target}, sample{acquiring,dt,rate,decayRate})`→`{progress,locked,justLocked}` — acquiring advances progress by `dt/rate` (capped 1), else decays by `dt/decayRate` (floored 0); `justLocked` fires only on the <1→≥1 crossing so the impure caller hangs lock-tone/haptic/`lockFlash` off it; combat.js `updateLockOn` does the THREE cone/range geometry then feeds the booleans/scalars in. `clearLockIf(lock{target,candidate,progress}, deadTarget)`→plain lock — on enemy death decides whether the player's lock clears (lockedTarget==`target`, lockTarget==`candidate`); entities.js `clearLocks(e)` delegates to it + applies the result instead of poking `player.lock*` directly (leak removed), `tests/targeting.test.js`.** **No THREE/store/DOM here — keep it require-safe.** |
| `js/roster.js` | **Jet roster DATA + ace-pool helpers — pure & require-safe (loaded right after `core.js`, before `globals.js`).** The `JETS` roster (per-airframe id/shape/name/stats/ability/skin colours) + `aceShapePool`/`jetNameForShape`; CommonJS export footer → `tests/ace-pool.test.js` + `tests/has-special.test.js` `require()` the REAL roster (no mirror/scrape). Lifted out of `globals.js` (it was bloating the THREE-coupled state file) and `entities.js` (which was forcing the ace-fn mirror). **Keep pure — no THREE/store/DOM, or it stops being require-safe.** |
| `js/globals.js` | Shared runtime STATE + data tables (the impure half; pure cores → `js/core.js`). State: `player`, `enemies`, `missiles`, `wave`, `run`, tech, `weather` live modifiers + `weatherT`/`weatherSeed`, `bossRush`/`bossRushIndex`/`bossRushT0`, `tutorial`, `dailyMode`/`dailySeed`, `awacsUses` (calls spent this sector), `barrelRoll*` runtime, `camShake` + `shakeCam(amt)` (max-wins setter), `controlScheme` ('auto'(default)|'pointer'|'rate') + `controlSensitivity` (0.5–2.0), `aimAssist` (gun aim-assist toggle, default on) + `aimStrength` (1–5), `gfxQuality` (auto|low|medium|high) / `gfxTier` (resolved low|**medium**|high — Track B), hangar-preview orientation `previewYaw`/`previewPitch` + `previewZoom` (F2: 0.8×–2.5× dolly, reset to 1.0 on jet switch), `flightInput`/`touchBtns`/touch state. Tables/constants: `jetStats` (the `JETS` roster moved to `js/roster.js`), `TECH_TREE` lattice (`req` string OR array = any-of, `reqAll` = all-of; `reqSatisfied` lives in `js/core.js` (require-safe), imported by `tests/ground-war.test.js`), `FOG_BASE`, `BARREL_ROLL_INVULN/COOLDOWN/DURATION/THRESHOLD`, `GFX_CULL_FAR`=9000/`GFX_CULL_JET`=6500. **Operations campaign (revamp): `campaignMode` (true while flying a bounded level — the in-flight path) + `campaignOpId`/`campaignLevelIdx`/`campaignWavesLeft`/`campaignSnapshot`/`campaignPlayerOpId`/`campaignBossPhases`. `opMode` now means "Operations selected in hangar"; `campaignMode` is the distinct in-flight bounded path.** Impure call-site `refreshGfxTier()` (device read → `resolveQuality`). THREE vector helpers (`fwdOf`/`rightOf`/`dirToQuat` + scratch vectors) stay here (browser-coupled — this is why globals.js is NOT require-safe and the cores were lifted out). VISUAL LANGUAGE (orthogonal to the per-airframe paint skins in meta.js): `PALETTES` (5 colour schemes) × `SKIN_HUDFONT` (6 visual languages — `standard`=default plain / `futuristic` / `analog` / `manual` / `flat` / `blueprint`) + `activePalette`/`activeSkin` + `applyPalette`/`applySkin` (set `<html data-palette/data-skin>`, sync canvas `HUD` triplets + `HUDFONT`, persist `skystrike_palette`/`skystrike_skin`; **default skin flipped futuristic→`standard`**). Each `[data-skin]` in styles.css restyles BOTH the hangar/menu chrome AND the in-play HUD instruments (speed/alt/thrust become physical gauges). |
| `js/i18n.js` | EN/ZH localization: `LANG`, `I18N` dict, `t(key)` for static strings, `jetText(jet, field)`/`techText(node, field)`/`metaText(node, field)` for data-table strings (fall back to EN/object field if no ZH override; `metaText` reads the `meta` group — perk/achievement/skin names+descs, which live ONLY in i18n since the meta.js tables carry no text fields) |
| `js/engine.js` | Three.js scene/renderer/terrain (`terrainH`); filmic pipeline (sRGB + ACES, physical lights) + PCFSoft sun shadows (`updateSunRig` follows player, ticked in `animate`); sky/sea shaders (`skyMat`/`seaMat`, `applyTimeOfDay`), billboard cloud banks (`retintClouds`/`updateClouds`), shader-detailed smooth terrain (fbm via onBeforeCompile); canvas FX textures `glowTex`/`cloudPuffTex`/`fireTex`/`tracerTex`; shared ordnance assets (`buildAssets`: merged-geometry missile via `mergeGeos` — matte/metallic tapered cylinder hull (cool-grey player / dark warm-grey enemy, NO neon emissive — trim/seeker bands are plain matte paint), `buildMissileMesh(enemy)` adds a SMALL constant warm rear thruster flame (`mslExhaust` fireTex sprite, no tracking halo); `spawnMissileTrail` lays a THIN white/grey smoke contrail; crossed-quad tracer streaks); `buildEnvMap` (PMREM sky → `scene.environment`), `updatePlayerShadow` blob shadow; weather+TOD gameplay (`applyWeather`/`updateWeather`: per-condition `scene.fog.density` from `FOG_BASE`×`fogMul`, storm sky/cloud darken + occasional lightning via `empFlash`; idempotent — recomputed from the TOD baseline; re-applied at the tail of `applyTimeOfDay` so TOD+weather compose); `applyGfxQuality()` (F11: 3-branch `GFX_SHADOW` lookup — `sun.shadow.mapSize` low 1024 / med+high 2048, `shadow.camera.far` 3000/4500/6000 by `gfxTier`, disposes the old shadow map to force realloc — VISUAL-ONLY, called in `initThree` + on settings change; tail-calls `applyEnvTier`). **Track B ENVIRONMENT TIERS: `applyEnvTier()` (idempotent orchestrator — `retuneTerrain`+`retuneSea`+`buildGroundObjects`+re-`applyWeather`); tier-aware `buildTerrain`/`buildTerrainGeo` (per-tier `TERRAIN_TIER` SEG + `terrainDetailH` visual displacement on Med/High + detail-aware normals; Low=current) + `retuneTerrain` (geo swap on tier change); tier-aware `buildScenery`/`buildSeaMat` (per-tier `SEA_TIER` SEG + wave/fragment-normal/reflectance/foam GLSL baked per variant, PRESERVES the tonemapping/colorspace tail; `foamMaskTex` precomputed coastline mask, `userData.shared`) + `retuneSea`; `applyWeather` fog density now `fogDensityFor(gfxTier,type)` (dramatic active-weather draw-distance cut); GROUND OBJECTS (NET-NEW, InstancedMesh): `buildGroundObjects`/`clearGroundObjects` (one InstancedMesh per type = 1 draw call; templates `groundObjTemplates()` built once+cached+`userData.shared`; placed from `planGroundObjects(weatherSeed,gfxTier,terrainH)`; High casts+receives shadows, Med receives only; per-arena `groundObjGroup` torn down in `clearArena` + rebuilt in `startGame`/`enterOperationRun`/`returnToHangar`).** |
| `js/airframes.js` | **Per-airframe `SHAPES` geometry-spec table + flag-normalization + enemy shape pools (`FIGHTER_SHAPES`/`ACE_SHAPES`) — pure & require-safe (loaded before entities.js).** Literal config (len/nose/wing/htail/vtail/intake + dozens of feature flags) consumed at call-time by `buildJet` (entities/combat/main) + the hangar preview; CommonJS footer → `tests/plain-shapes.test.js` + `tests/npc-airframes.test.js` import the REAL tables (no source scrape). Extracted from `entities.js`. (`ACE_SHAPES` appears unused — kept on the move.) **Keep pure — no THREE/store/DOM.** |
| `js/entities.js` | `createEnemy`, jet mesh building (`buildJet`, cached; `loftFuselage` superellipse hull w/ chines + engine decks, `intakeDuctGeo`), per-type update fns (`updateEnemy/Bomber/Drone/Ground`), `clearLocks`, `disposeGroup`; `markShadowCasters`, `patchJetSurface` (hull noise + panel lines + two-tone), `animEngines` (throttle-driven afterburner: flame/core/shock-diamonds + `userData.heatMat` nozzle glow). Per-airframe SHAPES carry generic feature flags consumed by flag-blocks inside `buildJet` (e.g. `tailBoom`/`levcon`/`tunnel`/`stinger`/`nacelleSplit`/`chineRidge`/`sawtoothTE`/`tipRail`/`tipPivot`/`lambdaFairing`/`finFillet`/`finBoom`/`dorsalHump`/`dorsalIntake`/`buriedExhaust`/`noseSpike`/`hostileLights`/`facetNose`/`noCanopy`/`flyingWing`/`canardCant`+`canardDihedral`/`semiIntake`/`shoulderIntake`/`chinSplit`/`bigLerx`/`wingFold`/`bayDoors`/`bayWide`/`sawtoothDoors`/`spineGun`). Near-duplicate spec names are aliased to one impl in the post-SHAPES block (`chineLine`/`noseChineBlend`→`chineRidge`, `sawtooth`/`sawtoothDoors`→`sawtoothTE`, `tipRails`→`tipRail`). New intake styles: `'dorsal'` and `'none'` (fall-through). CCAJET stores its drone V-tails under `ccaVtail` (not `vtail`) to satisfy the plain-shapes test. Boss AI (`updateEnemy`) reads `e.phase` (set in combat.js): fire-cadence multiplier + extra missiles per salvo scale at phase ≥2 / ≥3 (was the old `e.enraged` boolean). `updateBossSpecials`/`fireBossAttack` live in main.js and also key special cadence + attack intensity off `e.phase`. **glTF HERO MODELS (High tier): `jetGLTF{}` templates loaded by `loadJetModels()` (vendored `THREE.GLTFLoader` from `vendor/GLTFLoader.js`). Driven by the `JET_MODELS` table `{shapeId:{file,rotY,len}}` — ALL roster shapes have a `.glb` (f22 su57 j20 f35 eft tejas rafale fa18 j36 f47 j50 std + bomber). Each model is centred, `rotateY` per-shape to point nose→-Z (the models do NOT share a native orientation — `rotY` was derived+verified via a top-down contact-sheet render; nose -X→-π/2, +X→+π/2, +Z→π), scaled to `len` (fighters 26, BOMBER 44), geo/mats tagged `userData.shared` so disposeGroup spares the template. `loadJetModels()` early-returns on Low tier (no model memory on mobile). `buildJetOrGLTF(color,accent,cfg,hero,opts)` returns a glTF clone for shapes in `jetGLTF` on High tier (`gfxTier!=='low'`) else `buildJet`; `opts.enemy` adds a per-instance red foe-tint, `cfg.id==='F22' && !enemy` adds the afterburner rig. `cloneJetGLTF(id,accent,opts)` nests the scaled model in an outer game-space group and adds a per-jet afterburner via `addBurner(g, burn, accent)` — `burn{z,y,xs[],rad}` is computed per template in `loadJetModels` from the oriented bbox + `SHAPES.engines` (nozzles at the tail +Z, one plume per engine: 1/2/3, `rad` halved for buried/flying-wing exhaust), refined by a per-shape `BURN_OVERRIDE` table (`xw` spacing / `dy` height / `dz` aft — tuned visually against each model's nozzles). Plume builds the `userData.engines` contract `animEngines` drives (player/wingmen modulate by throttle; enemies keep the cruise plume). `applyFoeTint` clones materials PER INSTANCE (templates are shared — never mutated) and sets a red emissive sheen so enemy jets read as hostile. `TEXTURELESS_SHAPES` (F47/J20/J36/J50/STD/EFT/FA18 — geometry-only or flat-albedo, would render colourless) are recoloured in-code by `applyPaint(inner, paint)` from the chosen skin (`meta.js SKINS` carries paint ONLY for these 7 + their jets; the textured airframes have no skins and keep their baked liveries). **`paint` is EITHER a colour NUMBER (enemy-red / legacy) OR a skin descriptor `{color, zones}`: `zones` maps `material.name`→colour so designed liveries (splinter/two-tone/tiger) paint distinct hull sections WITHOUT textures/UVs (the exports have no usable UVs — per-material zoning is the technique). Material clones stay `shared:false` (disposeGroup frees them); NO textures allocated for the colour-block path → no per-spawn cost.** Both designed skins (2 & 3) are colour-block `zones` liveries — independent colourways, no decal/texture-overlay subsystem. The skin descriptor threads via `buildJetOrGLTF(color,accent,cfg,hero,{skin})`→`cloneJetGLTF(...,opts.skin)`→`applyPaint`; `createPlayer` passes `jetPaint(j)` (OWNED skin only — never the hangar `previewSkin`). Textureless enemies take the enemy-red `color` directly (opts.skin undefined → `applyPaint(inner, redNumber)`; no separate foe-tint). Used by `createPlayer` + `selectJet` (player: all shapes glTF + flames) and `createEnemy` (bomber always; fighters only when the spawn site sets `opts.useGLTF` — aces/hostile-aces/rivals do, mooks+drones+boss stay procedural). NO control surfaces (that deflecting overlay was removed). Verify orientation+flames+colour with `scripts/verify-jets.mjs` + `scripts/gltf-contact-sheet.mjs`. Dev tools: `scripts/gltf-{smoke,inspect,inspect-all,preview,contact-sheet}.mjs`, `scripts/measure-jet.mjs`.** |
| `js/combat.js` | Player update, weapons, missiles, damage, `killEnemy`, specials. Multi-phase boss state machine: `damageEnemy` drives `e.phase` (int, starts 1) via `nextBossPhase` (core.js) and steps through `bossEnterPhase(e,ph)` for each crossed threshold — bumps `turnRate`, fires a visual cue (`empFlash` + body/core emissive flare + phase-3 `spawnSmoke` armor shed) + banner (`banner.bossPhase2/3`) + guarded `shakeCam` (`if (typeof shakeCam==='function')`); fires once per phase (guard is idempotent + monotonic). Reads the `flightInput` seam (analog touch/motion) added on top of digital keyboard before clamping; fires `haptic(...)` on gun/missile/lock/kill/damage. The seam is interpreted per `controlScheme` via pure `steerCommand` (core.js): `'rate'` = legacy roll-rate; `'pointer'` = bank-hold to a held bank angle + auto-level on release (uses `currentBank = atan2(-right.y, up.y)`); `'auto'` = like pointer but capped at `STEER.autoMaxBank` with the heading turn applied as a WORLD-axis yaw (`∝ sin(currentBank)*autoYawGain*tb`, premultiplied onto `player.group.quaternion` after the body-frame integrate) — DECOUPLED from pitch so dive/climb works while turning (diagonals). Barrel-roll: `updatePlayer` ticks `barrelRollCooldown`/`barrelRollAnim`, consumes `barrelRollRequest` flag (set by main.js/controls.js), grants `player.invuln`, fires `showBanner`+`haptic`. The roll is a PURE body-Z roll (`q1.setFromAxisAngle(ZAX, rollRate*dt)`) with the pitch/yaw integrate skipped for its duration, so a full 360° leaves heading+pitch UNCHANGED (no final-orientation drift; the old mid-roll pitch pulse was removed). AIM ASSIST (on by default; `aimAssist` toggle + `aimStrength` 1–5 slider shown only while on): after the orientation integrate, `updatePlayer` premultiplies a bounded WORLD-axis nudge onto `player.group.quaternion` toward the `interceptPoint` of `pickGunTarget()` (the SAME gun lead the pipper draws) via pure `aimAssistCfg`+`aimAssistStep` (core.js) — cone+range gated, rate-capped (never a snap), yields during a barrel roll; the strongest tier forces onto the lead unless the player is steering (`manualAim`); uses `aimT1..aimT3` scratch. AWACS support calls (F10) — **COOLDOWN-GATED, FREE (no RP cost) as of balance 2026-06**: `nearestNonBossEnemy()` (nearest live non-boss, any direction) + `awacsAction(key)` — reads a seconds clock (`performance.now()/1000`), calls the pure decision `awacsResolve` (core.js) with `{uses: awacsUses, last: awacsLast}, AWACS_COOLDOWNS, AWACS_USES_MAX, key, now` → `{ok, reason, uses, last, effect, banner}`, commits `awacsUses`+`awacsLast` (NO `player.tp` deduction — calls used to compete with the permanent TECH_TREE for the same RP pool and were never worth it), then IMPERATIVELY applies the resolved `effect` (state mutation only): `strike`→`killEnemy(nearestNonBossEnemy(), true)` + `empFlash`; `resupply`→refill `bullets`/`flares`/`missiles` to max; `jam`→`player.jammer = max(player.jammer, AWACS_JAM_TIME)` (reuses the SPECTRA jam pathway in `updateMissiles`); then `showBanner(t(res.banner))` + `audio.power()`. Failure shows `res.banner` (`awacs.cooldown`/`awacs.empty`) or a neutral blip for an unknown key. The whole "allowed? which effect? which message?" decision is pure + tested (`tests/awacs-adapter.test.js`). **APEX PREDATOR cheat-death is now ONCE PER RUN (balance 2026-06): `player._cheatUsed` reset only in `startGame`, NOT per wave.** |
| `js/main.js` | Game loop (calls `readFlightInput()` before `updatePlayer`), wave scheduling, spawn fns (`airSpawnPos`/`groundSpawnPos`/`spawnGroundAt` helpers, `queueStrikeSite` builds fortified ground sites + fleeing convoys), wingman/CCA AI; first-touch hook calls `initTouchControls` (controls.js). **Wave cadence (balance 2026-06):** Endless boss spawns on a WINDOWED schedule (`bossWaveNext`/`bossWaveActive`, re-rolled 3-5 waves out via `nextBossOffset` after each boss) — NOT the old `wave % 4` metronome; the non-boss spawn guards key off `!bossWaveActive`. Density via `waveCount` (cap raised 10→16, `WAVE_COUNT_CAP`; cull handles draw cost). Occasional non-boss `isWildcardWave` spike (denser swarm + extra ace, `banner.wildcardWave`). `handleWaves` opens the tech shop on a CADENCE — `opMode || shouldOpenTechScreen(wave, lastWaveWasBoss)` (Operation always opens it: `deployFromTech`→`openOpMap` is the ONLY path to the next sector; Endless skips wave 1, then every 2nd wave + after any boss, RP banks in `player.tp`). Keydown wires AWACS calls (keys 1/2/3 → `Digit1`/`Digit2`/`Digit3`) → `awacsAction('strike'|'resupply'|'jam')`; `nextWave` resets `awacsUses`+`awacsLast` per sector (F10; codes added to `GAME_CODES`). `cullDistantEnemies()` (F11) hides distant enemy meshes via `e.group.visible` on the low gfx tier (never despawns — locks/markers/AI keep running; `GFX_CULL_FAR`=9000 ground / `GFX_CULL_JET`=6500 air; bosses/rivals always drawn), ticked after the enemy update loop in `animate`. **Operations campaign (revamp): bounded LEVEL path is a DISTINCT branch (endless scheduler untouched) — `nextWave` `if (campaignMode && campaignOpId)` builds the plan from `levelPlan(currentCampaignLevel())` (authored-absolute) and sets `campaignBossPhases`→`e._phaseCfg` on the spawned boss; `handleWaves` bounded-clear branch decrements `campaignWavesLeft`→`campaignLevelComplete()` on boss death or count≤0 (NO mid-level tech screen). Boss fire seam reads `e._fireMul`/`e._extraMissiles` (set by combat.js `bossEnterPhase` from `e._phaseCfg`).** |
| `js/hud.js` | **Canvas HUD renderer** (extracted from the ui.js god-file; loaded after combat.js, before ui-hud.js). The `drawHUD` entry + its `draw*` family — `drawGunPipper`/`drawStarObjectives`/`drawWeatherChip`/`drawWingman`/`drawHorizon`/`drawLockReticle`/`drawEnemy`/`drawRadar`. Pure canvas I/O: reads global game state (`player`/`enemies`/`run`/`hudK()`/the 2D `ctx`) and writes the canvas — NO DOM. `drawHUD` is the single entry, called from the game loop. (`pickGunTarget`/`hudK`/`spawnHitMarker`/`spawnDamageNumber`/`weatherLabel` stayed in ui-hud.js — not part of the canvas-render family.) |
| **ui.js god-file split (2026-06-15)** | The old 1669-line `js/ui.js` was split into the 5 `ui-*.js` files below (all browser globals, no imports — load order among them is irrelevant, but all load after `hud.js` and before `controls.js`). **Edit/read only the relevant file, not all five.** Canvas HUD renderer already lives in `js/hud.js`. |
| `js/ui-hud.js` | (1/5) In-game DOM HUD + camera: `updateCamera`/`cycleCamera`/`projectPoint`/`pickGunTarget`/`hudK`/`spawnHitMarker`/`spawnDamageNumber`/`weatherLabel`, el cache (`cacheEl`/`g`/`tog`), tutorial (`renderTutorial`/`startTutorial`/`finishTutorial`/`advanceTutorial`), `showBanner`, `updateWingmanSidebar`/`updateAwacsHud`, `updateDom`. `hudK()` (0.6–1.6) is the canvas-HUD scale for the "UI size" setting (world-projected markers keep EXACT screen position, only sizes scale). `updateDom` also publishes the INSTRUMENT SEAM each frame — `instrumentState` (core.js) → CSS custom props on `#hud` (`--spd/alt/thr-frac`, `--spd/thr/alt-deg`, `--alt-deg-k`, `--spd-kt`/`--alt-ft`) that the per-skin gauges (analog dials, blueprint/flat/manual instruments) render from. The instrument markup hooks live in the HUD's `.inst`/`.gauge`/`.needle`/`.ticks` spans (index.html). |
| `js/ui-tech.js` | (2/5) Tech-tree screen + frontier draft + armory + op-map launch: `reqSatisfied` (pure logic now in `js/core.js`; called here), `draftFrontier`/`rollDraftOffer`/`nodeState`/`openTechScreen`/`renderTechTree`/`renderDraftBar`/`togglePin`/`rerollDraft`, `renderArmory`/`switchTechTab`, `buyNode`/`commitNode`, wing picker (`openWingPicker`/`confirmWingPick`), `openOpMap`/`launchSector`/`applyDepot`/`deployFromTech`. |
| `js/ui-hangar.js` | (3/5) Hangar shell: `renderKillBoard`, `buildHangar`, `renderPilotPanel` (callsign/emblem handlers), `initOnboarding`, jet carousel (`selectJet`/`cycleJet`/`renderJetCard`), special-2 picker (`setSpecial2`/`renderSpecial2Picker`), `setDifficulty`/`setTimeOfDay`, manual tabs (`showManualTab`/`openManual`/`abortMission`), settings subtabs (`showSettingsSubtab`). **Controls&Manual/Settings entry (F7/F3): the sole `#manualBtn` is a GLOBAL persistent top-right overlay button — a BODY-LEVEL child (NOT inside any screen) so `position:fixed` anchors to the viewport and it never scrolls with a scroll container (the old in-`#hangar` copy was trapped by `#hangar`'s `backdrop-filter` containing block and scrolled away on scrollable menus — Jun-17 fix). CSS `.manual-fixed` (z-80 above all menu overlays; `env(safe-area-inset-*)` clears the iOS notch); visibility is driven EVERY FRAME by `syncManualBtn()` (ui-hangar.js, called at the top of `animate()`): shown on every menu (`state==='hangar'`), hidden in flight/gameover and behind the lang/onboard/`#manual` gates via the `.mb-hide` class. Verify with `scripts/verify-manualbtn.mjs`. It + pause (`#btnPause`/H/ESC→`toggleManual`) open `#manual`, whose always-visible tab row includes a labeled `Settings` tab (`#manTab_settings`) — settings is one tap from both menu and pause. Labels: `hangar.manualBtn` "⚙ CONTROLS & MANUAL", `hud.pause` "⚙ PAUSE · SETTINGS" (EN+ZH).** **Pre-launch reflow: the hangar CTA `#launch` reads "SELECT <jet>" (`refreshLaunchSub` now writes `tf('hangar.selectJet',{n})` into the button + blanks `#launchSub`; re-synced by `selectJet`) and opens `#modeChoice` (`openModeChoice`, gated by `launchBlocked()`). `#modeChoice` offers Endless (`opMode=false`→`openEndlessSetup`) or Operation (`opMode=true`→`openOperationsSelect`). `#endlessSetup` hosts the difficulty (`.dbtn`)/environment (`.tbtn`) selectors moved off the hangar + an explicit START (`opMode=false`→`startGame(selectedJet)`). Difficulty/env `.dbtn`/`.tbtn` wiring is unchanged (global selectors). `setOpMode` + the old `#modesel`/`.mbtn` toggle were removed — `opMode` is driven by the mode-choice buttons. Nav: `#modeChoice`→hangar, `#endlessSetup`→`#modeChoice`; `returnToHangar` hides both.** Visual-style picker = a 6-card gallery (`buildSkinGallery`/`selectSkinCard`/`refreshSkinGallery`, `SKIN_IDS`) replacing the old `<select id=setSkin>` — each card carries its own `data-skin` and renders a live in-style mini HUD preview (speed/alt/thrust); selection calls `applySkin` (persists) + roving-tabindex keyboard nav; labels via `t('skin.<id>')`/`.d`, refreshed by `applyLang`. **Jet-paint preview + buy-to-preview (per-airframe paint skins, distinct from the HUDFONT gallery above): the 3D preview is an ISOLATED renderer hosted INSIDE the selected jet's info card (NOT the shared hangar scene). `ensurePreviewRenderer()` lazily builds ONE second `THREE.WebGLRenderer` (its own `previewScene`/`previewCamera`/lights, `alpha:true`, sRGB+ACES+exposure to match engine.js, `setPixelRatio(devicePixelRatio)` for retina sharpness, `ResizeObserver`→`resizePreview`, own rAF `previewLoop` that idles off-hangar) bound to a persistent `previewCanvas`; `renderJetCard` prepends a `#jetPreview3D` hero host and `mountPreviewCanvas()` reparents the one canvas into the live card each selection (canvas survives innerHTML rebuilds — no context loss). `selectJet` builds `previewJet` via `previewPaint(JETS[i])` (transient `previewSkin` OR equipped), centres it at origin, swaps it into `previewScene` (NEVER the shared `scene`), and `disposeGroup`s the old one on every swap. `layoutPreviewJet()` is now a no-op stub. Drag-to-rotate (`main.js initPreviewDrag()`) gates pointerdown on `previewCanvas` (move/up on window), updates `previewYaw`/`previewPitch` (pitch clamped ±`PREVIEW_PITCH_MAX`), auto-rotate resumes ~3s after release; the spin/bob now lives in `previewLoop`, not the main `animate` loop (the shared `scene` no longer holds `previewJet`, so the main hangar backdrop + HUD are unaffected — fixes the old HUD-bleed/retina-blur bug). The `.jetpreview3d` host is enlarged (CSS height clamp 280–400px) and carries a side-mounted ZOOM slider (`#jetZoom`, `t('hangar.zoom')`, `wireZoomSlider()`): `previewLoop` dollies `previewCamera` toward origin by `1/previewZoom` (globals.js, 0.8×–2.5×, NO FOV change) each frame; `selectJet` resets `previewZoom`→1.0 on a real jet switch (the rebuilt slider re-syncs from it). `ui-flow.js startGame` tears the preview model out of `previewScene` + `disposeGroup`s it. `renderJetMeta` chips have 4 states — equipped `.on` / owned / locked / `.locked.preview` (previewing-but-unowned) — + inline `#jetMetaBuy`; `onJetMetaClick` previews ANY chip (owned→also `setSkin`, unowned→preview only, NO meta write) and the BUY CTA `buySkin`→`setSkin`. `renderJetMeta` also renders a livery-NAME readout (`#jetSkinName`, `.jmskinname`); `onJetMetaHover` (delegated `mouseover`/`focusin` on `#jetStage`) surfaces the hovered/focused chip's name generically via `metaText({id:'skin.'+id},'name')` (accessible — chips are native `<button>`s). `launchBlocked()`/`refreshLaunchGate()` gate the LAUNCH button while previewing an unowned skin (also enforced in `ui-flow.js startGame`+`launchLevel`); `returnToHangar` clears `previewSkin`. Verify with `scripts/verify-skin-gate.mjs` (headless gate+drag asserts) + `scripts/shot-hangar.mjs`. |
| `js/ui-flow.js` | (4/5) Meta screen + run lifecycle: `renderJetMeta`, `openMetaScreen`/`renderPerkGrid`/`renderAchGrid`, `startGame` (resets `awacsUses` per run, F10), `gameOver`/`endRun`/`operationComplete`, boss rush (`startBossRush`/`bossRushComplete`/`refreshBossRushEntry`), daily challenge (`loadDaily`/`dailyToday`/`startDaily`/`refreshDailyEntry`). **Operations campaign (revamp): the 3 navigation screens + lifecycle — `openOperationsSelect`/`renderOperationsSelect`, `openLevelMap`/`renderLevelMap`, `openBriefing`/`campaignLoadoutSummary`, `launchLevel` (builds the op player on first entry via `enterOperationRun`, snapshots tp/score, sets `campaignMode`+`opSector`+`campaignWavesLeft`); `campaignLevelComplete` (rewards + `campaignClearLevel` + unlock; boss → `operationComplete`), `campaignLevelFailed` (rollback tp/score to `campaignSnapshot`, back to map), `clearCampaignArena` (between-level teardown that KEEPS the player). `gameOver` guards into `campaignLevelFailed` when `campaignMode`; `startGame` still routes opMode→`openOperationsSelect` (now reached from the `#modeChoice` Operation button rather than a hangar toggle — see the ui-hangar.js pre-launch reflow row). Tech shopping is between levels (level-map R&D button → `openTechScreen`; `deployFromTech` returns to the map).** |
| `js/ui-settings.js` | (5/5) Best/settings persistence + i18n apply + arena teardown: `loadBest`/`saveBest`/`updateBest`, `loadSettings`/`saveSettings` (graphics-quality F11: `gfxQuality` whitelist auto|low|**medium**|high; load calls `refreshGfxTier`+`applyGfxQuality`), `applyLang` (full DOM relocalize — incl. AWACS touch buttons `tb-aws`/`tb-ars`/`tb-ajm` via `touch.aws|ars|ajm`, and `#lblGfx`+`set.gfx*` segbtns), mobile-control widgets `bindSeg`/`syncControlSettingsUI`/`enableMotionFlow`/`installMotionStatus` (`#motionNote`), `applyHudScale()` (pushes `--hud-scale` to DOM + `hudK()` to canvas), `clearArena` (also frees the per-arena Track B `groundObjGroup` via `clearGroundObjects`)/`returnToHangar` (rebuilds ground scatter for the hangar view). |
| `js/controls.js` | Unified flight-input layer (loads after ui-settings.js, before main.js). `flightInput = {pitch, roll}` SEAM: `readFlightInput()` selects active analog source by `mobileControl` and writes it each frame (combat.js adds keyboard on top + clamps). Pure shaping helpers `shapeAxis`/`mapFlightInput`/`motionAxis`/`emaSmooth` + the `AGGRESSION` preset table now live in `js/core.js` (require-safe; `tests/controls.test.js` imports them); controls.js consumes them. Floating virtual joystick (`initTouchControls` — spawns under thumb on left-half touch) is DIRECTIONAL — the jet flies toward the stick (up=climb, down=dive, left/right=bank that way; touch pitch uses `+touchInput.y` because combat.js applies `pitchIn=-pitchCmd` and `+pitchRate`=nose-up). `invertY` semantics differ by input: on TOUCH it flips the WHOLE stick (one sign `s` negates both pitch+roll → jet flies opposite the stick); on KEYBOARD (combat.js) it flips pitch only; motion keeps `!invertY` pitch shaping. Setting label `set.invert` = "Invert controls". Action-button binding, `applyButtonStyle` (opacity var + layout class). Motion: `deviceorientation` listener, iOS `requestMotionPermission()` gesture flow, `recenterMotion`, landscape axis mapping. `haptic(ms)` guarded by `haptics` setting + `navigator.vibrate`. Motion redo (2026-06-14): EMA smoothing (`emaSmooth`, core.js), no-data watchdog + `window.onMotionStatus` status seam (rendered in ui-settings.js `#motionNote`); landscape swap / recenter / iOS `requestMotionPermission` unchanged. Barrel-roll touch: `joyEnd` detects double-flick release (|x|>0.6) within `BARREL_ROLL_THRESHOLD` and sets `barrelRollRequest`. AWACS touch buttons (F10): `bindBtn` wires `tb-aws`/`tb-ars`/`tb-ajm` → `awacsAction('strike'|'resupply'|'jam')`. THR slider (mobile): `bindThrSlider`/`paintThrSlider` REPLACE the old momentary THR/BRK buttons — a vertical drag track (`#tb-thr`, with `#tb-thr-fill`/`#tb-thr-thumb`/`#tb-thr-lbl`) that sets `player.throttle` (0..1) DIRECTLY on touch. combat.js no longer ramps throttle on touch (`touchBtns.thr`/`brk` stay false there, ramp inert; keyboard Shift/Ctrl throttle + the `highG` brake-turn are unchanged on desktop, but `highG` is no longer touch-triggerable). Repainted each frame from `player.throttle` in `readFlightInput`; label localized via `tb-thr-lbl` (`touch.thr`) |
| `js/rival.js` | Nemesis persistence/escalation. require-safe (store used only inside fns) + CommonJS export footer → helpers imported by `tests/rival.test.js` (with a `global.store` stub). F8: also hosts `HOSTILE_ACES` pool + `hostileAceFor(sectorType)` / `hostileAceDeltas(entry)` pure fns, imported by `tests/ace-pool.test.js` (`aceShapePool`/`jetNameForShape` now live in require-safe `js/roster.js` and are imported directly — no longer mirrored from THREE-coupled `entities.js`) |
| `js/meta.js` | PERSISTENT meta-progression (loads after rival.js): `meta` state (SP currency + owned jets/skins/perks/achievements) saved via `store.get/set('skystrike_meta')`, `loadMeta`/`saveMeta`/`validMeta` (mirror rival pattern). `spAward(run, player)` PURE — derives end-of-run SP from the existing `run` stats (callers stamp `run.waveReached`/`run.rivalLevel` first; called in `endRun`). `applyMetaPerks(player)` applies owned `META_PERKS` levels at run start (called in `startGame` after `createPlayer`, BEFORE in-run TECH_TREE). Tables `META_PERKS`/`SKINS`/`ACHIEVEMENTS`/`EMBLEMS` (text lives in i18n `meta` group, resolved via `metaText`). Buy/query API: `perkCost`/`buyPerk`, `jetUnlocked`/`buyJet`, `skinOwned`/`buySkin`/`selectedSkin`/`setSkin`/`jetPaint`/`resolveSkinPaint`, `grantAch`/`checkAchievements`, `spBalance`/`bankSP`. **`SKINS` shape: each textureless jet → 3 entries — `default` is PLAIN `{id,color,accent}` (fast path); skins 2 & 3 are COLOUR-BLOCK liveries `{id,accent,color,zones}` (`zones`={material.name→colour}, per-material zoning, no textures) — two independent colourways (e.g. cool splinter vs warm signature), distinct at a glance. `resolveSkinPaint(jet,id)` resolves ANY id → `{color,accent,zones}` (ownership-agnostic; `zones` null for `default`); `jetPaint(jet)=resolveSkinPaint(jet, selectedSkin(jet.id))` is OWNED-only (gameplay). The transient hangar `previewSkin` (globals.js, UI-only) is NEVER read here — it can't leak into a launched jet.** Star objectives (F6): `evalStars(run, player)` PURE → 0..3 from three live `run` conditions (kills ≥`STAR_KILL_FRAC`×waves·4 / `run.cleanWaves`≥1 no-damage wave / `run.missions`≥1) + `bestStars(meta, jetId, stars)` records the per-jet lifetime best in `meta.stars[jetId]` (monotonic). **Per-mission star conditions (Ops, F2 stars rework): `starCondMet(cond, run)` PURE — maps ONE descriptor `{type,n?}` to one real `run` stat (`kills`/`clean`/`objective` = the evalStars defaults; `noDamage`=`damageTaken===0`; `accuracy`=`hits/shots*100≥n`; `flawless`=objective+no-damage). `evalStarsFor(run, player, conds)` sums up to 3 met conds (capped 0..3) and FALLS BACK to `evalStars` when `conds` is missing/empty — so non-annotated levels + Endless are byte-identical. A level row may carry an optional `stars:[{type,n?}…]` (≤3) override; `ui-flow.js endRun` reads `currentCampaignLevel().stars` and renders the per-condition checklist (`stars.cond.<type>` i18n, EN+ZH). Endless/Daily DEATH hides the star UI and shows a performance rating (kills/accuracy/waves) via `#go_rating`. Mirrored in `tests/stars.test.js`.** Pilot identity (F13): pure `sanitizeCallsign(str)` (≤8 A–Z0–9) + `EMBLEMS` table (gate free/sp/ach) + pure `emblemUnlocked(id, meta)` + `buyPatch`/`setEmblem`/`setCallsign`. Boss rush (F15): `freshMeta` also defaults `bossRushUnlocked:false` + `bossRushBest:0`; `loadMeta` heals legacy saves missing them (`typeof !== 'boolean'/'number'` → default). `freshMeta` defaults `stars{}`, `callsign:''`, `emblem:'wings'`, `patches{}`; `loadMeta` heals legacy saves missing ANY of these (validMeta stays lenient — back-compat, NO progression wipe). `meta` shape now `{v, sp, jets{}, skins{}, perks{}, ach{}, sel{}, stars{}, callsign, emblem, patches{}, bossRushUnlocked, bossRushBest}`. require-safe + CommonJS export footer → cores imported by `tests/meta.test.js`/`tests/stars.test.js`/`tests/grading.test.js` (store-touching fns run against a `global.store` stub; boss-rush trio imported from `core.js`). RP (`player.tp`) stays the separate in-run currency. **Operations campaign (revamp): `freshMeta` adds `campaign:{}`; `loadMeta` heals it leniently (`if (!meta.campaign||typeof!=='object') meta.campaign={}` — no wipe, no version bump, `validMeta` untouched); thin store-touching wrappers `campaignOpUnlocked`/`campaignLevelUnlocked`/`campaignLevelState`/`campaignClearLevel` over the core.js PURE progression. Per-op shape `campaign[opId]={unlocked,furthest,levels:{[levelId]:{cleared,bestScore,bestStars}}}`.** |
| `js/opmap.js` | Operation map node logic. `sectorPlan(type, wave)` now returns each plan's legacy spawn fields PLUS a `mission` descriptor (`sectorMission(type)`: FURBALL→sweep, INTERCEPT→intercept, STRIKE→strike, ELITE→elite, DEPOT→none, FINAL→boss) AND a reserved `weather` slot (`null`; feature #4 weather reads/sets it per sector) AND `hostileAce` boolean (F8: true for all combat sectors except DEPOT/FINAL). require-safe + CommonJS export footer → `OPERATIONS`/`levelPlan`/`sectorMission`/`sectorPlan` imported by `tests/op-map.test.js`. **Operations Map revamp (2026-06): `genOpMap` REMOVED — the campaign is the `OPERATIONS` data table (3 ops × 8/8/9 linear levels; level row `{id,nameKey,type,loreKey,objectivesKey,enemyIntelKey,waves,spawn{fighters,aces,bombers,ground,weather,tod,hostileAce},isBoss,boss{callsignKey,introKey,phases[3]}}`, plus an OPTIONAL `objectives:[{type,wp?,spawn?}]` multi-phase sequence on the 6 previously-repetitive nav levels — walked by missions.js, passed through by `levelPlan`) that drives map render + briefing + spawns; pure `levelPlan(lvl)` builds the AUTHORED-ABSOLUTE plan (NOT `sectorPlan(type,wave)`, whose wave-scaled branches are dead at small bounded waves).** Scripted set-pieces (F14): pure core imported by `tests/setpiece.test.js` — `SETPIECES` data table (samCorridor/bomberRun/carrier, each `mission`/`ground`/`bombers`/`convoy` + name/intro/outro i18n keys) + `setpieceFor(type, stage)` (RETIRED to `null` in the Operations revamp — set-pieces are now opt-in per level row via `lvl.setpiece`, folded by `levelPlan`) + `setpiecePlan(id, base)` (PURE fold of an encounter onto a base plan, NEW object, base untouched, tags `setpiece`) + `setpieceOutcome(id, won)` (win→encounter outro key, fail→`banner.missionFailedObj`) |
| `js/missions.js` | Typed-mission state machine (loads after opmap.js, before combat.js). require-safe + CommonJS export footer → PURE CORE imported by `tests/missions.test.js`: `MISSIONS` table (sweep/intercept/escort/defend/strike + recon/stealth — all SEVEN are real typed missions, cores `reconWon`/`stealthWon`/`stealthFailed` in core.js), `missionForSector`, `startMission`/`missionKill`/`tickMission`. Runtime glue (browser-only): `mission` global state, `startSectorMission(plan, wave)` (sets `mission`, spawns escort convoy via `escortUnit` trucks / defend asset via `defendAsset` radar; ELITE rolls escort|defend), `updateMission(dt)` (ticked in `animate`; recomputes escort survivors/defend asset-hp, ticks pure machine, resolves win/fail), `objectiveText`, `missionSiteDown()` (strike win from `killEnemy`), `onMissionResolved` (win→RP/score bonus + `run.missions++` + `clearMissionLeftovers`; fail→`gameOver`). `missionKill(mission,e)` called from `combat.js killEnemy`; a mission-active sector blocks `handleWaves` clear until it resolves. Set-pieces (F14): runtime flag `setpieceActive` (id of the authored encounter running this sector, or null) — set in `startSectorMission` from `plan.setpiece`; when active it leads with the encounter's `intro` banner instead of the generic objective header, and `onMissionResolved` shows `setpieceOutcome(id, won)` (opmap.js) on win. Cleared on resolve + in `startGame`. **Multi-phase objective SEQUENCE (Operations objective pass 2026-06): a level row's optional `objectives:[{type,wp?,spawn?}]` queue is walked phase-by-phase — `startSectorMission` inits `missionPhases`/`missionPhaseIdx` and starts phase 0 via `startMissionPhase` (builds the phase mission, trims nav `wp`, sets sweep/intercept target to the phase's spawn budget, spawns props via shared `spawnMissionProps` + per-phase fighters/bombers/strike-site, fires the Req D `fireObjectiveCallout`); `onMissionResolved(won)` advances to the next phase (pure `nextObjectivePhase` in core.js) on each win, clearing leftovers between phases, and only runs the sector-complete path after the LAST phase. Any phase fail = level fail. Converted nav levels carry `waves:1` so the sequence runs in one wave. Tests: `tests/objective-phases.test.js` + headless `scripts/verify-objective-phases.mjs`; audit `scripts/audit-flyto.mjs`.** |
| `js/storage.js` | ONLY place allowed to touch `localStorage` — enforced by `tests/storage.test.js`. Use `store.get/set/remove` |

## Skystrike / Frontend
After UI/CSS or i18n changes, perform visual verification by opening the page in the browser before considering the task done. Use `node scripts/shot.mjs` for headless screenshot confirmation of graphics changes.

## Hard rules
- All new user-facing strings go through `t(key)` (static UI) or `jetText`/`techText` (JETS/TECH_TREE rows); add EN+ZH entries in `js/i18n.js`.
- No direct `localStorage` outside `js/storage.js` (test-enforced).
- No ES modules / `import` — globals only, respect load order. The ONLY sanctioned module syntax is a **CommonJS export footer** — `if (typeof module !== 'undefined' && module.exports) module.exports = {…}` — on require-safe files (`core.js`/`roster.js`, plus footers on `opmap`/`missions`/`meta`/`rival`) so Node tests can `require()` the real implementation instead of carrying byte-identical copies. It is inert in the browser (`module` is undefined there). New pure logic should go in `core.js` and be imported by its test, NOT mirrored.
- Three.js is vendored; never re-add CDN script tags. r128-era API calls go through shims in `engine.js`.
- Geometry/materials may be shared via cache — use `disposeGroup` for cleanup; never dispose marker geometry. `disposeGroup` skips geometry AND materials tagged `userData.shared` (cached jet geometry, ASSET missile hull/trim/sprite mats) — tag anything new that multiple live objects share.
- Enemy death/despawn must call `clearLocks(e)` and remove marker.
- Custom `ShaderMaterial` fragment shaders must end with `#include <tonemapping_fragment>` + `#include <colorspace_fragment>` or they won't match the scene's ACES/sRGB grading.

## Docs layout (docs/)
- *(empty — ios/ docs removed with the Capacitor scaffold)*

## Current state

Game is **feature-complete** for web. Playable at `index.html`.

**All systems implemented (merged to master):**
- Flight/combat: touch + motion + optional desktop **mouse-pointer flight** (point-to-fly, mirrors the joystick), mobile THR slider (drag-to-throttle, replaces THR/BRK buttons), 3 control schemes (AUTO/ASSISTED/EXPERT), barrel-roll (i-frames now span the FULL 0.65s roll, not just 0.4s — projectile-immune the whole roll), camera shake, gfx quality tiers; F-47 CCA-SWARM drone count scales with missile-count techs (`mslSwarm`). ANALOG STEERING (joystick/mouse/motion) is WORLD-RELATIVE + auto-leveling: the jet flies toward the stick in SCREEN space and never inverts — pitch is referenced to the world horizon, the turn is a world-up yaw, and bank auto-levels to wings-level when centered (combat.js `directional` branch in `updatePlayer`, applies for analog regardless of the AUTO/ASSISTED/EXPERT scheme, which now only shapes KEYBOARD). Left-click fires the gun (`mouseFiring`)
- Meta-progression: SP currency, perks, jet/skin unlocks (**only the FT-1 trainer (the default jet) is unlocked at a fresh start — `STARTER_JETS = ['FT-1']` in `js/meta.js`; every other jet incl. F-22 and SU-57 starts LOCKED and is SP-purchasable via `buyJet`. `selectedJet` defaults to the FT-1's roster index 0 in `js/globals.js`**), **SP-gated 2nd special slot** (`meta.slot2`/`SLOT2_COST`/`slot2Unlocked`/`buySlot2`; fire equipped slot-2 with **B**), achievements, pilot callsign + emblem, run grading (S/A/B/C), star objectives; **"Unlock all aircraft (dev)" (`devUnlockAll`) now bypasses EVERY gate** — jets, skins, emblems, boss-rush, and the 2nd-slot unlock — for playtesting
- Missions: 7 typed mission types (sweep/intercept/escort/defend/strike/recon/stealth), sector boss aces, scripted set-pieces (samCorridor/bomberRun/carrier), AWACS support; **multi-phase objective sequences (the 6 nav levels run an ordered heterogeneous queue, e.g. recon→strike→intercept, advancing on each objective win) with a 3s center objective CALLOUT (Req D, EN+ZH) + a distinct orange fly-to-waypoint marker (Req B)**
- **Operations campaign (linear revamp, 2026-06): 3 operations (IRON VEIL / MIDNIGHT MERIDIAN / SUNFIRE HORIZON; 8/8/9 levels) authored as a DATA table in `opmap.js`. Flow: operations-select → linear level map (locked/unlocked/cleared nodes) → full Mission Briefing Screen → bounded level (wave count scales with level index, cap 4). Checkpoint economy: RP/tech persist across levels within an operation; WIN commits + grants `grantLevelRewards`, DEATH rolls tp/score back to the pre-level snapshot (level stays unlocked, free retry), cleared levels are replayable + re-grant (`CAMPAIGN_REPLAY_REWARDS`, farmable by design). Each operation's boss is the final node with an authored 3-phase fight signature (`e._phaseCfg`: turnMul/fireMul/extraMissiles/pattern/flags — standoff/headOn/dive/chaff/mirror). Progress persisted in `meta.campaign` via the storage seam (legacy saves heal, no wipe). The bounded wave flow is a DISTINCT `handleWaves` path — Endless/Daily/Boss-Rush unchanged. New pure logic + tests in `tests/campaign.test.js`.**
- Game modes: daily seeded challenge, boss rush (unlocks after first operation boss clear)
- Content: weather + TOD gameplay, multi-phase bosses, named hostile aces, sound variety (per-jet engine timbre, lock/kill SFX)
- UX: tabbed flight manual (4 panels per tab; the Guide tab documents **B = 2nd special slot**), **scrollable ally/wingman/CCA sidebar** (`#wingSidebar` max-height + overflow-y), tutorial opens immediately after controls screen, bilingual EN/ZH throughout
- **HUD/UX/units batch (2026-06-17):** (1) Waypoint fly-to marker plays a one-shot chime (`audio.ping`, engine.js) the frame each waypoint checks off (missions.js recon/stealth tick) + scales with distance (`drawMissionWaypoint` `wpScale`: 2.0× at arrival → 0.6× far). (2) Mission intro CARD (`#missionCard`; `showMissionCard`/`dismissMissionCard` in ui-hud.js) at mission start — type name + mechanical desc (`mission.desc.<verb>`) + per-level lore blurb; FIRST encounter (storage `skystrike_seenMissionType_<verb>`) is interactive + persists until tap, REPEAT auto-dismisses 5s (ticked in `updateDom`). Persistent top objective readout shifted below `#btnPause` (hud.js y 14→44, detection bar + stars follow). (3) UNITS: `unitSystem` ('imperial' mph+ft default / 'metric' kph+m) — pure `speedDisplay`/`altDisplay` (core.js), Settings→Display "Units" segmented control (`#unitsTog`, bindSeg in ui-hangar.js), labels via `applyUnitLabels` (`hud.mph`/`hud.kph`/`hud.ft`/`hud.metres`). (4) Destroy-site BUGFIX: strike completion now driven by the live mission (`strikeSiteResolves` core.js: `strikeWaveActive || missionType==='strike'`) not the level-wide `strikeWaveActive` flag — multi-phase STRIKE phases (firstLight/blindspot/ghostSignal, top-level `spawn.ground:false`) no longer hang. (5) Lore: per-level blurb key derived from nameKey via `levelBlurbKey` (opmap.js) → 25 unique EN+ZH `op.<id>.l<N>.blurb` strings; operation-lore panel (`#opLore`; `openOperationLore` in ui-flow.js) on op-select tap → Enter (→level map) / Back. New pure tests `tests/strike-site.test.js` + `tests/units.test.js`; headless `scripts/verify-units-card.mjs`.
- **Lore + global-gear follow-up (2026-06-17, opening-screen version → 1.1):** (1) Fix 1 — the Controls&Manual gear (`#manualBtn`) is now a BODY-LEVEL global overlay pinned to the viewport top-right across every menu (root cause was `#hangar`'s `backdrop-filter` trapping the old in-`#hangar` `position:fixed` child → it scrolled with content). `.manual-fixed` z-80 + `env(safe-area-inset-*)`; `syncManualBtn()` (ui-hangar.js) toggles `.mb-hide` each frame from `animate()`. Verify `scripts/verify-manualbtn.mjs`. (2) Fix 2 — operation lore (F5 `#opLore`) expanded to 4–5 paragraphs per op (`op.<id>.lore` ×3) and mission blurbs (F2 `#missionCard`) to ~2 paragraphs per level (`op.<id>.l<N>.blurb` ×25), EN+ZH (ZH machine-drafted, flagged for native review); paragraphs are `\n\n`-separated, rendered via `white-space:pre-line`. F5 modal body scrolls (`#opLore .brief-body` overflow-y:auto) while title + BACK/ENTER stay pinned; `.mcard` gets a safety `max-height/overflow`.
- **UX pass 2 (2026-06-17):** (1) Mission lore relocated — the 2-paragraph `blurb` now renders on the pre-launch BRIEFING screen as the `#briefLore` SITUATION text (`white-space:pre-line`); the in-flight `#missionCard` shows ONLY the concise objective + how-to (`mission.desc.<verb>`), blurb hidden in `showMissionCard`. **(blurb dedupe 2026-06-18): the blurb is shown ALONE — `openBriefing` no longer prepends the old one-line `loreKey`/`boss.introKey`, which duplicated the blurb's own opening; lore/intro is now only a fallback when a level has no blurb.** (2) `#versionTag` ("Version 1.1") moved to a BODY-LEVEL viewport-fixed bottom-right element (rides scroll) shown on the language-select screen + every menu, hidden in flight/gameover (toggled by `syncManualBtn`). **The language-select panel also carries a `#langVersion` "Version: &lt;n&gt;" readout (`.ls-version`), text synced from `#versionTag` in `initOnboarding` so one literal drives both.** (3) HUD top declutter: pilot tag (`.pilot-tag` top 98→104) sits immediately under the HP/SHD/THR bars, the canvas weather chip drops below it (`drawWeatherChip` y 86→136), and `#warns-status` moved top `--sp-8`→`120px` to clear the centre objective readout (y44) + star checklist (y72-117). (4) Hangar pilot identity (`#pilotPanel`) moved ABOVE the jet carousel as a compact single horizontal row (old `.loadout` wrapper removed, `#callsignHint` hidden). (5) Hangar background is now flat opaque `var(--base)` (no see-through 3D scene); the jet preview keeps its own isolated canvas inside the card.

**iOS:**
- Capacitor scaffold removed from repo (regenerate with `npx cap add ios` when ready to build)
- Build pipeline: `npm run build:www` copies source → `www/`; then `npx cap sync ios` after adding the platform
- Pending before App Store: swap `js/storage.js` internals to `@capacitor/preferences`, device smoke test

**Known open issues:**
- Device smoke tests pending (headless Playwright cannot simulate touch/orientation/fps)
- Reset-path material leak (geometry cache — see entities.js/disposeGroup notes)

## Keep this file current
This file replaces codebase exploration — stale info costs more tokens than no info (wrong assumptions → wasted edits/re-reads). When you change code, update the relevant line in the same commit:
- New/renamed/moved file, or new script-load-order entry → update Architecture table + load-order chain.
- New cross-file convention/helper (like `clearLocks`, `airSpawnPos`, storage seam) → add to Hard rules or the table.
- Finish/start a roadmap item → update Current state. Remove stale entries rather than letting them accumulate.

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).
