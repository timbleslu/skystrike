# SKYSTRIKE // ACE PROTOCOL

Arcade jet combat game. Three.js (r159, vendored), single HTML page, no build step, no framework, no modules.

**Source of truth: GitHub `timbleslu/skystrike`, worked via this clone. `git fetch`/`pull` before editing. Ignore any Obsidian Vault or `~/Downloads` copy of Skystrike — they are stale decoys, never source.**

## Run / Test
- Play: open `index.html` in a browser (or any static server).
- Mobile motion test: `npm run serve:https` — LAN HTTPS static server (self-signed cert; accept the warning on the phone). DeviceOrientation needs a secure context, so `file://` / plain http yields zero events.
- Tests: `npm test` — plain Node scripts in `tests/*.test.js`, no test framework. Each test extracts or mirrors functions from source files; keep mirrored helpers byte-identical with their source.
- Visual check: `node scripts/shot.mjs <outPrefix> [tod 0|1|2]` — boots the game headless (playwright devDep) and saves hangar/flight/fx/terrain screenshots. Use this to verify any graphics change.
- Close-up check: `node scripts/beauty.mjs <outPrefix> [jetIndex] [tod]` — UI-hidden hangar close-ups (jet front/side/rear + missile/tracer ordnance board). Use when judging model/material quality.

## Architecture (read this, skip exploration)
All code is browser globals — no imports/exports. Script load order in `index.html` defines availability:
`vendor/three.min.js → storage.js → globals.js → i18n.js → engine.js → entities.js → rival.js → meta.js → opmap.js → missions.js → combat.js → ui.js → controls.js → main.js`

| File | Role |
|---|---|
| `js/globals.js` | Shared state (player, enemies, missiles, wave, run, tech), math helpers (`rand`, `clamp`, `damp`), `jetStats` per-airframe table; `TECH_TREE` lattice (`req` string OR array = any-of, `reqAll` = all-of; `reqSatisfied` lives in ui.js, mirrored in `tests/ground-war.test.js`); `controlSensitivity` (0.5–2.0 turn-rate multiplier, Settings slider); weather core (`weather` live state + `WEATHER` table + pure `resolveWeather`/`turbSample`/`rollWeather` + `FOG_BASE`/`NIGHT_RADAR_MUL`, feature #4 — pure fns mirrored byte-identical between MIRROR markers in `tests/weather.test.js`); `controlScheme` ('pointer'|'rate') + `STEER` tunables + pure `steerCommand` (point-to-steer bank-hold/auto-level vs legacy roll-rate; mirrored in `tests/steer.test.js`); camera shake: `camShake` global + `CAMSHAKE_RATE`/`CAMSHAKE_K` tunables + `shakeCam(amt)` (max-wins setter) + pure `decayShake(v, dt)` (mirrored byte-identical in `tests/camshake.test.js`); barrel-roll tunables `BARREL_ROLL_INVULN/COOLDOWN/DURATION/THRESHOLD` + runtime state `barrelRoll*` + pure `rollDetect`/`rollCooldownGate` (MIRROR block, `tests/barrel-roll.test.js`); boss-phase core (`BOSS_PHASE2_HP`=0.6, `BOSS_PHASE3_HP`=0.3 + pure `bossPhaseFor(hpFrac)`→1/2/3 + once-per-phase guard `nextBossPhase(reached,hpFrac)` — monotonic, never regresses; mirrored byte-identical in `tests/boss-phase.test.js`); boss-rush core (F15): runtime `bossRush`/`bossRushIndex`/`bossRushT0` + `BOSS_RUSH_POOL`/`BOSS_RUSH_TOTAL` + pure `bossRushNext(index)` (boss type for a leg or null) / `bossRushDone(killed,total)` (saturating) / `betterTime(prev,next)` (keep lower; 0=no record) — mirrored byte-identical between MIRROR markers in `tests/meta.test.js`; first-run tutorial core (F5): `tutorial` runtime state `{active,step,done,prevShots,prevMissiles}` + `TUTORIAL_STEPS`/`TUTORIAL_DONE` + pure `tutorialNext(step,event)` (ordered, monotonic, skip→done, terminal; MIRROR block, `tests/tutorial.test.js`); daily-challenge core (F7): runtime flags `dailyMode`/`dailySeed` + pure `makeRng(seed)` (stateful mulberry32 — same `Math.imul` mixing family as `rollWeather`) and `dailySeedFor(y,m,d)` (stable int seed per calendar date, distinct dates → distinct seeds; NEVER reads the clock — caller passes y/m/d) — mirrored byte-identical between MIRROR markers in `tests/daily.test.js`; AWACS support calls (F10): `AWACS_COSTS`/`AWACS_USES_MAX`/`AWACS_JAM_TIME`=8 + runtime `awacsUses` (calls spent this sector) + pure `awacsCall(state, costs, max, key)` → `{ok, reason, rp, uses}` (deducts RP + bumps uses only when affordable AND under the per-sector cap; reasons `unknown`/`empty`/`noRp`/`ok`; mirrored byte-identical between MIRROR markers in `tests/awacs.test.js`); graphics-quality core (F11): `gfxQuality` setting ('auto'/'low'/'high', Settings segtog, persisted) + live `gfxTier` ('low'/'high') + `GFX_CULL_FAR`/`GFX_CULL_JET` cull radii + pure `resolveQuality(setting, dpr, isTouch)` (mirrored byte-identical between MIRROR markers in `tests/gfx-quality.test.js`) + impure `refreshGfxTier()` call-site (device read → resolveQuality) |
| `js/i18n.js` | EN/ZH localization: `LANG`, `I18N` dict, `t(key)` for static strings, `jetText(jet, field)`/`techText(node, field)`/`metaText(node, field)` for data-table strings (fall back to EN/object field if no ZH override; `metaText` reads the `meta` group — perk/achievement/skin names+descs, which live ONLY in i18n since the meta.js tables carry no text fields) |
| `js/engine.js` | Three.js scene/renderer/terrain (`terrainH`); filmic pipeline (sRGB + ACES, physical lights) + PCFSoft sun shadows (`updateSunRig` follows player, ticked in `animate`); sky/sea shaders (`skyMat`/`seaMat`, `applyTimeOfDay`), billboard cloud banks (`retintClouds`/`updateClouds`), shader-detailed smooth terrain (fbm via onBeforeCompile); canvas FX textures `glowTex`/`cloudPuffTex`/`fireTex`/`tracerTex`; shared ordnance assets (`buildAssets`: merged-geometry missile via `mergeGeos`, `buildMissileMesh(enemy)`, crossed-quad tracer streaks); `buildEnvMap` (PMREM sky → `scene.environment`), `updatePlayerShadow` blob shadow; weather+TOD gameplay (`applyWeather`/`updateWeather`: per-condition `scene.fog.density` from `FOG_BASE`×`fogMul`, storm sky/cloud darken + occasional lightning via `empFlash`; idempotent — recomputed from the TOD baseline; re-applied at the tail of `applyTimeOfDay` so TOD+weather compose); `applyGfxQuality()` (F11: sizes `sun.shadow.mapSize` 1024/2048 + `shadow.camera.far` 3000/6000 by `gfxTier`, disposes the old shadow map to force realloc — VISUAL-ONLY, called in `initThree` + on settings change) |
| `js/entities.js` | `createEnemy`, jet mesh building (`buildJet`, cached; `loftFuselage` superellipse hull w/ chines + engine decks, `intakeDuctGeo`), per-type update fns (`updateEnemy/Bomber/Drone/Ground`), `clearLocks`, `disposeGroup`; `markShadowCasters`, `patchJetSurface` (hull noise + panel lines + two-tone), `animEngines` (throttle-driven afterburner: flame/core/shock-diamonds + `userData.heatMat` nozzle glow). Per-airframe SHAPES carry generic feature flags consumed by flag-blocks inside `buildJet` (e.g. `tailBoom`/`levcon`/`tunnel`/`stinger`/`nacelleSplit`/`chineRidge`/`sawtoothTE`/`tipRail`/`tipPivot`/`lambdaFairing`/`finFillet`/`finBoom`/`dorsalHump`/`dorsalIntake`/`buriedExhaust`/`noseSpike`/`hostileLights`/`facetNose`/`noCanopy`/`flyingWing`/`canardCant`+`canardDihedral`/`semiIntake`/`shoulderIntake`/`chinSplit`/`bigLerx`/`wingFold`/`bayDoors`/`bayWide`/`sawtoothDoors`/`spineGun`). Near-duplicate spec names are aliased to one impl in the post-SHAPES block (`chineLine`/`noseChineBlend`→`chineRidge`, `sawtooth`/`sawtoothDoors`→`sawtoothTE`, `tipRails`→`tipRail`). New intake styles: `'dorsal'` and `'none'` (fall-through). CCAJET stores its drone V-tails under `ccaVtail` (not `vtail`) to satisfy the plain-shapes test. Boss AI (`updateEnemy`) reads `e.phase` (set in combat.js): fire-cadence multiplier + extra missiles per salvo scale at phase ≥2 / ≥3 (was the old `e.enraged` boolean). `updateBossSpecials`/`fireBossAttack` live in main.js and also key special cadence + attack intensity off `e.phase` |
| `js/combat.js` | Player update, weapons, missiles, damage, `killEnemy`, specials. Multi-phase boss state machine: `damageEnemy` drives `e.phase` (int, starts 1) via `nextBossPhase` (globals.js) and steps through `bossEnterPhase(e,ph)` for each crossed threshold — bumps `turnRate`, fires a visual cue (`empFlash` + body/core emissive flare + phase-3 `spawnSmoke` armor shed) + banner (`banner.bossPhase2/3`) + guarded `shakeCam` (`if (typeof shakeCam==='function')`); fires once per phase (guard is idempotent + monotonic). Reads the `flightInput` seam (analog touch/motion) added on top of digital keyboard before clamping; fires `haptic(...)` on gun/missile/lock/kill/damage. The seam is interpreted per `controlScheme` via pure `steerCommand` (globals.js): `'rate'` = legacy roll-rate; `'pointer'` = bank-hold to a held bank angle + auto-level on release (uses `currentBank = atan2(-right.y, up.y)`). Barrel-roll: `updatePlayer` ticks `barrelRollCooldown`/`barrelRollAnim`, consumes `barrelRollRequest` flag (set by main.js/controls.js), grants `player.invuln`, fires `showBanner`+`haptic`, and overrides `player.rollRate` for the spin duration. AWACS support calls (F10): `nearestNonBossEnemy()` (nearest live non-boss, any direction) + `awacsAction(key)` — wraps the pure `awacsCall` (globals.js) reading `{rp: player.tp, uses: awacsUses}`, commits the deduction, then applies the effect: `strike`→`killEnemy(nearestNonBossEnemy(), true)`; `resupply`→refill `bullets`/`flares`/`missiles` to max; `jam`→`player.jammer = max(player.jammer, AWACS_JAM_TIME)` (reuses the SPECTRA jam pathway in `updateMissiles`). Failure shows `awacs.noRp`/`awacs.empty` |
| `js/main.js` | Game loop (calls `readFlightInput()` before `updatePlayer`), wave scheduling, spawn fns (`airSpawnPos`/`groundSpawnPos`/`spawnGroundAt` helpers, `queueStrikeSite` builds fortified ground sites + fleeing convoys), wingman/CCA AI; first-touch hook calls `initTouchControls` (controls.js). Keydown wires AWACS calls (keys 1/2/3 → `Digit1`/`Digit2`/`Digit3`) → `awacsAction('strike'|'resupply'|'jam')`; `nextWave` resets `awacsUses` per sector (F10; codes added to `GAME_CODES`). `cullDistantEnemies()` (F11) hides distant enemy meshes via `e.group.visible` on the low gfx tier (never despawns — locks/markers/AI keep running; `GFX_CULL_FAR`=9000 ground / `GFX_CULL_JET`=6500 air; bosses/rivals always drawn), ticked after the enemy update loop in `animate` |
| `js/ui.js` | HUD canvas (`drawHUD`), menus, hangar, tech tree, settings (incl. mobile-control widgets: `bindSeg`/`syncControlSettingsUI`/`enableMotionFlow`), touch buttons. `startGame` resets `awacsUses` per run (F10); `applyLang` localizes the AWACS touch buttons (`tb-aws`/`tb-ars`/`tb-ajm` via `touch.aws|ars|ajm`). Graphics-quality setting (F11): `gfxQuality` in `saveSettings`/`loadSettings` (whitelist auto|low|high; load calls `refreshGfxTier`+`applyGfxQuality`), `bindSeg('gfxQualityTog',…)`, `syncControlSettingsUI` active-mark, `applyLang` localizes `#lblGfx`+segbtns (`set.gfx*`) |
| `js/controls.js` | Unified flight-input layer (loads after ui.js, before main.js). `flightInput = {pitch, roll}` SEAM: `readFlightInput()` selects active analog source by `mobileControl` and writes it each frame (combat.js adds keyboard on top + clamps). Pure shaping helpers `shapeAxis`/`mapFlightInput`/`motionAxis` + `AGGRESSION` preset table (mirrored byte-identical in `tests/controls.test.js`). Floating virtual joystick (`initTouchControls` — spawns under thumb on left-half touch), action-button binding, `applyButtonStyle` (opacity var + layout class). Motion: `deviceorientation` listener, iOS `requestMotionPermission()` gesture flow, `recenterMotion`, landscape axis mapping. `haptic(ms)` guarded by `haptics` setting + `navigator.vibrate`. Motion redo (2026-06-14): EMA smoothing (`emaSmooth`, mirrored byte-identical), no-data watchdog + `window.onMotionStatus` status seam (rendered in ui.js `#motionNote`); landscape swap / recenter / iOS `requestMotionPermission` unchanged. Barrel-roll touch: `joyEnd` detects double-flick release (|x|>0.6) within `BARREL_ROLL_THRESHOLD` and sets `barrelRollRequest`. AWACS touch buttons (F10): `bindBtn` wires `tb-aws`/`tb-ars`/`tb-ajm` → `awacsAction('strike'|'resupply'|'jam')` |
| `js/rival.js` | Nemesis persistence/escalation (helpers mirrored in `tests/rival.test.js`). F8: also hosts `HOSTILE_ACES` pool + `hostileAceFor(sectorType)` / `hostileAceDeltas(entry)` pure fns (MIRROR block, mirrored byte-identical in `tests/ace-pool.test.js`) |
| `js/meta.js` | PERSISTENT meta-progression (loads after rival.js): `meta` state (SP currency + owned jets/skins/perks/achievements) saved via `store.get/set('skystrike_meta')`, `loadMeta`/`saveMeta`/`validMeta` (mirror rival pattern). `spAward(run, player)` PURE — derives end-of-run SP from the existing `run` stats (callers stamp `run.waveReached`/`run.rivalLevel` first; called in `endRun`). `applyMetaPerks(player)` applies owned `META_PERKS` levels at run start (called in `startGame` after `createPlayer`, BEFORE in-run TECH_TREE). Tables `META_PERKS`/`SKINS`/`ACHIEVEMENTS`/`EMBLEMS` (text lives in i18n `meta` group, resolved via `metaText`). Buy/query API: `perkCost`/`buyPerk`, `jetUnlocked`/`buyJet`, `skinOwned`/`buySkin`/`selectedSkin`/`setSkin`/`jetPaint`, `grantAch`/`checkAchievements`, `spBalance`/`bankSP`. Star objectives (F6): `evalStars(run, player)` PURE → 0..3 from three live `run` conditions (kills ≥`STAR_KILL_FRAC`×waves·4 / `run.cleanWaves`≥1 no-damage wave / `run.missions`≥1) + `bestStars(meta, jetId, stars)` records the per-jet lifetime best in `meta.stars[jetId]` (monotonic). Pilot identity (F13): pure `sanitizeCallsign(str)` (≤8 A–Z0–9) + `EMBLEMS` table (gate free/sp/ach) + pure `emblemUnlocked(id, meta)` + `buyPatch`/`setEmblem`/`setCallsign`. Boss rush (F15): `freshMeta` also defaults `bossRushUnlocked:false` + `bossRushBest:0`; `loadMeta` heals legacy saves missing them (`typeof !== 'boolean'/'number'` → default). `freshMeta` defaults `stars{}`, `callsign:''`, `emblem:'wings'`, `patches{}`; `loadMeta` heals legacy saves missing ANY of these (validMeta stays lenient — back-compat, NO progression wipe). `meta` shape now `{v, sp, jets{}, skins{}, perks{}, ach{}, sel{}, stars{}, callsign, emblem, patches{}, bossRushUnlocked, bossRushBest}`. Pure fns mirrored byte-identical in `tests/meta.test.js` (+ `tests/stars.test.js`). RP (`player.tp`) stays the separate in-run currency |
| `js/opmap.js` | Operation map node logic. `sectorPlan(type, wave)` now returns each plan's legacy spawn fields PLUS a `mission` descriptor (`sectorMission(type)`: FURBALL→sweep, INTERCEPT→intercept, STRIKE→strike, ELITE→elite, DEPOT→none, FINAL→boss) AND a reserved `weather` slot (`null`; feature #4 weather reads/sets it per sector) AND `hostileAce` boolean (F8: true for all combat sectors except DEPOT/FINAL). Both `sectorMission`+`sectorPlan` mirrored byte-identical in `tests/op-map.test.js`. Scripted set-pieces (F14): pure core (MIRROR block, mirrored byte-identical in `tests/setpiece.test.js`) — `SETPIECES` data table (samCorridor/bomberRun/carrier, each `mission`/`ground`/`bombers`/`convoy` + name/intro/outro i18n keys) + `setpieceFor(type, stage)` (DETERMINISTIC node→encounter id or null, keyed on the FIXED genOpMap stage coords: STRIKE@1=samCorridor, ESCORT@4=bomberRun — same campaign node always triggers it) + `setpiecePlan(id, base)` (PURE fold of an encounter onto a base plan, NEW object, base untouched, tags `setpiece`) + `setpieceOutcome(id, won)` (win→encounter outro key, fail→`banner.missionFailedObj`) |
| `js/missions.js` | Typed-mission state machine (loads after opmap.js, before combat.js). PURE CORE (mirrored byte-identical between MIRROR markers in `tests/missions.test.js`): `MISSIONS` table (sweep/intercept/escort/defend/strike, each `setup`/`onKill`/`onTick`/`winFail`), `missionForSector`, `startMission`/`missionKill`/`tickMission`. Runtime glue (browser-only): `mission` global state, `startSectorMission(plan, wave)` (sets `mission`, spawns escort convoy via `escortUnit` trucks / defend asset via `defendAsset` radar; ELITE rolls escort|defend), `updateMission(dt)` (ticked in `animate`; recomputes escort survivors/defend asset-hp, ticks pure machine, resolves win/fail), `objectiveText`, `missionSiteDown()` (strike win from `killEnemy`), `onMissionResolved` (win→RP/score bonus + `run.missions++` + `clearMissionLeftovers`; fail→`gameOver`). `missionKill(mission,e)` called from `combat.js killEnemy`; a mission-active sector blocks `handleWaves` clear until it resolves. Set-pieces (F14): runtime flag `setpieceActive` (id of the authored encounter running this sector, or null) — set in `startSectorMission` from `plan.setpiece`; when active it leads with the encounter's `intro` banner instead of the generic objective header, and `onMissionResolved` shows `setpieceOutcome(id, won)` (opmap.js) on win. Cleared on resolve + in `startGame` |
| `js/storage.js` | ONLY place allowed to touch `localStorage` — enforced by `tests/storage.test.js`. Use `store.get/set/remove` |

## Hard rules
- All new user-facing strings go through `t(key)` (static UI) or `jetText`/`techText` (JETS/TECH_TREE rows); add EN+ZH entries in `js/i18n.js`.
- No direct `localStorage` outside `js/storage.js` (test-enforced).
- No ES modules / `import` — globals only, respect load order.
- Three.js is vendored; never re-add CDN script tags. r128-era API calls go through shims in `engine.js`.
- Geometry/materials may be shared via cache — use `disposeGroup` for cleanup; never dispose marker geometry. `disposeGroup` skips geometry AND materials tagged `userData.shared` (cached jet geometry, ASSET missile hull/trim/sprite mats) — tag anything new that multiple live objects share.
- Enemy death/despawn must call `clearLocks(e)` and remove marker.
- Custom `ShaderMaterial` fragment shaders must end with `#include <tonemapping_fragment>` + `#include <colorspace_fragment>` or they won't match the scene's ACES/sRGB grading.

## Docs layout (docs/)
- `working/superpowers/plans/` — active in-progress implementation plans (writing-plans skill). Deleted once merged; empty = nothing in flight.
- `working/superpowers/ideas/` — unscoped feature brainstorms awaiting design, e.g. `2026-06-11-feature-ideas-progression-content.md` (meta-progression: SP currency, jet unlocks, skins, achievements, mission variety).
- `ios/` — iOS transition docs: `ios.md` (build/run via Capacitor), `ios-completion-checklist.md` (remaining steps to finish `feat/ios-readiness`).
- `reference/` — system/architecture reference docs (currently empty; this CLAUDE.md is the primary reference and stays at repo root).

## Current state (2026-06-11)
- Branch `feat/ios-readiness`: iOS-readiness plan COMPLETE (all 6 tasks, plan doc deleted post-merge): npm scaffold, Three.js vendoring, storage seam, safe-area CSS, vendored fonts (`vendor/fonts/`), Capacitor iOS scaffold (`ios/`, SPM-based, no CocoaPods; build via `npm run build:www && npx cap sync ios`, see `docs/ios/ios.md`). Pending: browser/device smoke test, swap storage.js internals to `@capacitor/preferences` before App Store release, merge to main (see `docs/ios/ios-completion-checklist.md`).
- Graphics overhaul COMPLETE (2026-06-12, merged as v1): filmic color pipeline, real-time sun shadows, billboard clouds, smooth shader-detailed terrain, textured fireball/smoke FX. High-poly ordnance/jet pass: lofted superellipse fuselages (stealth chines, blended engine decks), clearcoat hero paint + iridescent canopies, panel-line shader, slime lights, shock-diamond afterburners, merged-geometry missiles, gradient tracer streaks. All 3 TODs verified via `scripts/shot.mjs` + `scripts/beauty.mjs`. Pending: in-browser perf check on a real device (headless shots can't measure fps).
- Tech tree v2 (2026-06-12): rebuilt as a woven lattice — OR-gate `req` arrays, AND-gate `reqAll` hybrids (dashed connectors), convergence spine at x3 (DATALINK→RHYTHM OF WAR→OVERLOAD COUPLING→SIPHON FIELD→OMEGA PROTOCOL), new mechanics in combat.js: `comboDmg`, `gunScavenge`, `slowOnKill`, `flakFlares`, `execBlast` (+ existing `shieldOnKill` now purchasable).
- Jet visual redesign pass done (2026-06-12): all 17 SHAPES re-planformed for airframe accuracy (F-22 diamond wing + tail booms, Su-57 LEVCONs/tunnel/stinger/nacelle pods, J-20 dihedral canards + fin booms, F-35 fat hull, FA-18 trapezoid + LERX/wing-fold, J-36 double-delta flying wing, J-50 lambda + tip-pivots, BOMBER B-2 flying wing, CCAJET noCanopy drone, etc.). New generic reusable flag-blocks inside `buildJet` (see entities.js row). All tests green; verified via `scripts/shot.mjs` + `scripts/beauty.mjs` (F22/SU57/J36/J50 close-ups).
- i18n + onboarding (2026-06-12): full EN/ZH localization via `js/i18n.js` (`t()`/`jetText()`/`techText()`); new first-run flow (Language Select → Controls/Instructions brief → hangar); Settings adds Language toggle + Control Sensitivity slider (`controlSensitivity`, applied to turn rate in combat.js). Flow now wired up (`initOnboarding` in ui.js, called from main.js boot): `#langSelect`/`#onboard` overlays gated by `onboarding` flag (global in globals.js, blocks hangar keyboard shortcuts in main.js) and persisted via `skystrike_onboarded` in storage; pre-existing players (detected via `skystrike_settings`) skip it. All tests pass.
- i18n completeness pass (2026-06-13): localized everything that was still EN-hardcoded — canvas HUD (`drawHUD`/`drawGunPipper`/`drawLockReticle`/`drawEnemy`: GUNS/SUPPLY/LOCKED/LOCKING/BOSS/BOMBER/distance units/Lv/ACE), DOM HUD (special label, lock chip, wingman sidebar RTB/EXP/s), all `showBanner(...)` callsites in main.js/combat.js/ui.js (banner.* + ks.* keys; ability-NAME banners like OVERDRIVE stay EN by callsign convention), the `#warns` warnings + `#hint` + `#btnPause` + touch buttons (set in `applyLang`), the full FLIGHT MANUAL body (new `manBody.*` HTML keys → `applyLang` sets `innerHTML` of `manUL_*`/`manP_*`), tech-tree/armory cost labels + hints, kill board, op-map sector labels, jet-card `gen` (via `genText`) + spec values (topSpeed/ceiling/cannon ZH overrides in `I18N.ZH.jet`). New helper `genText(gen)` in i18n.js. Verified EN+ZH via headless capture; all tests pass.
- Flight manual menu (2026-06-13): the `#manual` overlay is now tabbed instead of one long scroll — a `.mnav` of `.mnavbtn`s (Guide / Systems / Tactics / Settings) toggles `.mtab` panels (`showManualTab(name)` in ui.js, delegated click on `#manNav`; `openManual` resets to `guide`). Sections regrouped: Guide = Flight+Combat+Target Lock, Systems = Aircraft Stats+HUD&Radar, Tactics = Wingman+Enemies+Tech Tree, Settings = Settings. All existing `manH_*`/`manUL_*`/`manP_*` IDs preserved; new tab labels via `manual.tab*` i18n keys (EN+ZH) set in `applyLang`.
- Combat content pass (2026-06-13): 6 new TECH_TREE nodes (globals.js) with 5 new mechanics in combat.js/entities.js — `burnDps`/`burnTime`/`burnSpread` (INCENDIARY ROUNDS `g6` + INFERNO CASCADE `ic`: cannon-hit DoT that spreads on burn-death; ticked in `tickEnemyStatus`), `empKill` (EMP SUBMUNITIONS `u5`: kills stun nearby foes via `e.stun`, gated into `updateEnemy/Bomber/Ground/Drone` + boss specials + folded into `scrambled`), `frenzy`/`frenzyOnKill`/`frenzyMax` (KILL FRENZY `fz`: decaying fire-rate/damage stack via `frenzyAmt()`, HUD meter in `drawHUD`), `escortPD` (GUARDIAN ESCORTS `gd`: wingmen run point-defense in `updateMissiles`), plus SCRAMJET CORE `p5` (stats). Point-defense laser now has VISUALS: pooled additive bolts `firePdBeam`/`updatePdBeams` in combat.js (player PD + escort PD). Wingmen now drawn on the main HUD (`drawWingman` in ui.js — teal chevron + health pip + order tag + off-screen arrow). New feature WINGMAN COMMANDS: `T` focus-fire / `Y` regroup (`wingCommand` in main.js, `w.forced`/`w.defend` honoured in `updateWingman`/`updateCCA`; `KeyT`/`KeyY` in GAME_CODES). All strings EN+ZH (banner.wing*/hud.frenzy|focus|guard + tech ZH g6/ic/u5/p5/fz/gd). All tests pass.
- Touch + motion controls (2026-06-14, branch `feat/techs-laser-hud-wingmen`): new `js/controls.js` unified input layer (see Architecture row). THE SEAM is `flightInput = {pitch, roll}` (globals.js), written each frame by `readFlightInput()` and consumed in combat.js (analog added on top of digital keyboard, then clamped). New settings (globals.js + Settings tab, persisted via `saveSettings`/`loadSettings` storage seam): `mobileControl` (touch/motion), `motionAggression` (casual/balanced/direct — `AGGRESSION` table tunes deadzone/expo/sens/maxAngle/autoLevel/pitchClamp), `invertPitch`, `haptics`, `buttonOpacity` (0.4–1.0), `buttonLayout` (right/left/compact). Touch stick is now floating (spawns under thumb on left-half touch). Motion = `deviceorientation` with iOS `requestMotionPermission()` user-gesture flow + recenter + landscape mapping; deny/unsupported falls back to Touch with a localized notice. Haptics fire on gun/missile/lock/kill/damage. All new strings EN+ZH (`set.*` keys); CSS adds `--btn-opacity`, `.layout-*`, `.segtog/.segbtn`. New `tests/controls.test.js` (mirrors shapeAxis/mapFlightInput/motionAxis byte-identical + asserts preset ordering invariants). All tests green. Pending: device smoke (headless can't simulate touch/orientation). This is feature 1 of a 4-feature arc (controls → meta-progression → missions+bosses → weather; a later weather feature will feed a turbulence offset into `flightInput`).
- Meta-progression (2026-06-14, branch `feat/techs-laser-hud-wingmen`): new `js/meta.js` PERSISTENT layer (feature 2 of 4; see Architecture row). Second currency **SP** (distinct from in-run RP `player.tp`), banked each run end and spent between runs. THE SEAM for downstream features: `spAward(run, player)` (PURE) derives end-of-run SP from the existing `run` stats object — `endRun()` (ui.js) stamps `run.waveReached = wave` + `run.rivalLevel = rival.level` then calls `spAward` + `bankSP` + `checkAchievements`; `run` stays the single run-stats source (feature 3 missions reads/extends the same object). `meta` state `{v, sp, jets{}, skins{}, perks{}, ach{}, sel{}}` persists via `store.get/set('skystrike_meta')` (key + `validMeta` guard mirror rival.js). `applyMetaPerks(player)` applies owned `META_PERKS` levels in `startGame` right after `createPlayer`, BEFORE in-run tech. UI (ui.js + index.html + styles.css): hangar SP chip (`#hangarSp`, `updateSpHud`), end-screen SP earned/banked (`#go_sp`/`#go_spTotal`), new `#meta` tabbed overlay (Upgrades perk tree + Achievements grid, opened via hangar `#metaBtn`/`openMetaScreen`), jet-card lock+skin overlay (`#jetMeta`/`renderJetMeta`, delegated `onJetMetaClick` on `#jetStage`); locked jets gate in `startGame`+`selectJet`, skins applied via `jetPaint` in `selectJet`/`createPlayer`. Starter jets FT-1/F-22/SU-57 free; others 250 SP; skins 120 SP. All strings EN+ZH (`meta.*` static keys + the i18n `meta` data group resolved by `metaText`). New `tests/meta.test.js` (mirrors `spAward`/`applyMetaPerks`/`perkCost`/`validMeta`/buy fns byte-identical + behavioral asserts). All tests green. Pending: device/browser smoke (headless playwright not installed here).
- Known open issue: reset-path material leak (see geometry cache notes).
- Controls redesign (2026-06-14, branch `feature/controls-redesign`): (1) Joystick gains a **point-to-steer** scheme (new default) alongside **classic** rate-stick — `controlScheme` global ('pointer'|'rate') + `STEER` tunables + pure `steerCommand` in globals.js; combat.js `updatePlayer` interprets the `flightInput` seam per scheme (pointer = bank-hold to a held bank angle + auto-level on release via `currentBank = atan2(-right.y, up.y)`; rate = legacy roll-rate, byte-identical). Settings adds a Steering segtog (`#controlSchemeTog`, persisted, EN+ZH `set.*`). Keyboard path untouched. (2) Mobile **motion redo**: EMA low-pass smoothing (`emaSmooth`, α0.2), a no-data watchdog + `window.onMotionStatus` status seam surfaced in `#motionNote` (requesting/denied/unsupported/no-data/live/off), reusing the already-correct landscape swap + recenter + iOS requestPermission-from-tap. Root cause of "motion dead in browser" was **no secure context** → new `npm run serve:https` (scripts/serve-https.mjs, self-signed LAN HTTPS) for phone testing. New `tests/steer.test.js` + `emaSmooth` mirror in controls.test.js; all tests green. Pending: on-device smoke (headless can't simulate touch/orientation); playtest pointer feel (tune `STEER.bankGain`/`maxBank`).
- Missions + boss aces (2026-06-14, feature 3 of 4): new `js/missions.js` — typed-mission layer over the op-map. 5 mission types (sweep/intercept/escort/defend/strike) + FINAL sector boss graduates from the nemesis system (`spawnRival` is the cap; defeat advances the op-map via `rivalDefeated`, escape escalates via `rivalEscaped`). `sectorPlan` now carries a `mission` descriptor + reserved `weather` slot (feature #4 seam). Scheduler in `nextWave` (main.js) calls `startSectorMission` + spawns per type (`spawnInterceptTarget` tags bombers `_missionTarget`); `updateMission(dt)` ticked in `animate`; `missionKill`/`missionSiteDown` hooked into `killEnemy` (combat.js). Objective HUD readout (top-centre) + start/win/fail banners in ui.js (`drawHUD`). Mission win pays RP/score, increments `run.missions` (new `run` field), and feeds a new `tactician` achievement (meta.js ACHIEVEMENTS, +EN/ZH `ach.tactician`). All strings EN+ZH (`banner.missionStart|missionComplete|missionFailedObj`, `mission.name.*`, `mission.*` HUD labels). `mission` reset to null in `startGame`. All tests green incl. new `tests/missions.test.js` (+ op-map/meta mirrors updated byte-identically). KNOWN: mission fail (intercept timeout / escort losses / defend asset lost) ends the run via `gameOver` — minimal "sector outcome" per spec; no partial-fail/op-map-rollback flow (YAGNI).
- Mission grading S/A/B/C (2026-06-14): pure `gradeRun(run, player)` in `js/meta.js` — weighted score (kill efficiency 40%, time 20%, damage taken 25%, objectives 15%) maps to letter S/A/B/C with SP multipliers 1.5/1.3/1.15/1.0. `run.damageTaken` field added to BOTH run initializers (globals.js + ui.js `startGame`), incremented in `combat.js damagePlayer`. `endRun` (ui.js) stamps `run.timeSecs = secs`, calls `gradeRun`, banks `Math.round(award * grade.mult)` SP, renders `#go_grade` (`.grade-letter` big + `.grade-bonus` "SP BONUS xN"). HTML: `#go_grade` div in `#gameover`; CSS: `.gograde`/`.grade-letter`/`.grade-bonus` styles. i18n: `grade.title`/`grade.bonus` EN+ZH. `applyLang` sets `#goLblGrade`. New `tests/grading.test.js` (mirrors `gradeRun` byte-identical + asserts near-perfect→S, poor→C, null→C, monotonic mults, only S/A/B/C, score in [0,1]). All tests green.
- Barrel-roll evasive maneuver (2026-06-14, F9): double-tap Q or E (keyboard) or double-flick the joystick laterally (touch) to trigger a ~0.4s 360° spin + `player.invuln = 0.4` i-frames + `banner.evade` flash + 80 ms haptic. 6s cooldown. Implementation: pure helpers `rollDetect`/`rollCooldownGate` + constants `BARREL_ROLL_INVULN/COOLDOWN/DURATION/THRESHOLD` in globals.js (MIRROR block); runtime state `barrelRollCooldown/Anim/Request/LastKeyTap/LastTouchTap` also in globals.js. Keyboard double-tap wired in main.js keydown handler; touch flick-release wired in controls.js `joyEnd`. combat.js `updatePlayer` ticks the timers, consumes `barrelRollRequest`, and overrides `player.rollRate` to `TWO_PI / BARREL_ROLL_DURATION` for the spin duration (cleans up via normal damp). State reset in `startGame` (ui.js). i18n: `banner.evade` EN+ZH. New `tests/barrel-roll.test.js` (MIRROR byte-identical + constant/gate/combined asserts). All tests green.
- Weather + TOD gameplay (2026-06-14, feature 4 of 4 — COMPLETE; closes the 4-feature arc): weather is now tactical, not cosmetic. `weather` live state + `WEATHER` table (clear/fog/storm) in globals.js; pure core `resolveWeather(type,tod)` (folds the `NIGHT_RADAR_MUL` night factor at TOD index 2), `turbSample(t,amp)` (bounded, zero-mean attitude wobble) and `rollWeather(seed)` (deterministic standalone roll, weighted clear). `engine.js applyWeather(type)` copies the resolved modifiers into `weather` + drives visuals through the existing TOD/fog/cloud path (idempotent: `scene.fog.density = FOG_BASE * fogMul`, storm sky/cloud darken); `updateWeather(dt)` advances the turbulence phase (`weatherT`, globals) + fires occasional storm lightning via `empFlash`; `applyTimeOfDay` now re-applies the weather overlay at its tail so TOD+weather compose. Modifiers consumed where they matter: `combat.js` lock acquire range ×`lockRangeMul` + lock-on time ×`lockSpeedMul` (folds into the existing `player.lockSpeedMul` AESA seam) + turbulence added onto the `flightInput` seam (feature #1) then clamped; `ui.js` radar drops contacts beyond `6500 × radarMul` + a HUD weather chip (`drawWeatherChip`/`weatherLabel`, top-left). Selection is mission-tied: `opmap.js sectorPlan` now returns a real `weather` + `tod` per sector type (FINAL = night storm); `main.js nextWave` calls `applyWeather`+`applyTimeOfDay` at op-sector start (condition named in the sector banner) and rolls `rollWeather(weatherSeed + wave)` per wave in standalone (seed reset in `startGame`). All strings EN+ZH (`weather.clear|fog|storm|night`). New `tests/weather.test.js` (WEATHER invariants + resolveWeather night factor + turbulence bounds/zero-mean + rollWeather determinism/weighting; pure core mirrored byte-identical) + op-map mirror updated. All tests green. Pending: in-browser visual smoke across 3 TODs × weather (headless playwright not installed here — engine fns runtime-verified against a THREE stub instead).
- Multi-phase bosses (2026-06-14, feature F4): bosses now shift behavior at HP thresholds instead of a single enrage flag. The lone `e.enraged` boolean is replaced by an `e.phase` integer (starts 1, set in `createEnemy`). Pure core in globals.js (`bossPhaseFor`/`nextBossPhase` + `BOSS_PHASE2_HP`=0.6, `BOSS_PHASE3_HP`=0.3) — `damageEnemy` (combat.js) computes the HP-implied phase via the once-per-phase guard and steps through `bossEnterPhase(e,ph)` for each crossed boundary. Each transition fires **exactly once** (guard is monotonic + idempotent — survives HP oscillation near a boundary, and a big hit can skip straight to phase 3), bumps aggression (`turnRate`), and fires a visual cue (`empFlash` + body/core emissive ignition flare + phase-3 `spawnSmoke` armor shed) + banner (`banner.bossPhase2/3`, EN+ZH; `banner.bossEnraged` retained) + guarded screen shake (`if (typeof shakeCam==='function') shakeCam(...)` — `shakeCam` not yet merged). Old enrage difficulty preserved as phase ≥2 (AI fire cadence + extra missiles in entities.js `updateEnemy`; special cadence + `fireBossAttack` intensity in main.js now key off `e.phase`), with phase 3 pushing further. Phase machine is reusable by the Boss-Rush mode (F15, now built). New `tests/boss-phase.test.js` (boundary phases, monotonicity, once-per-phase no-double-trigger under oscillation; pure core mirrored byte-identical). All tests green. Pending: in-browser playtest of phase-transition feel (headless can't render the cue).
- Boss rush mode (2026-06-14, F15 — COMPLETE): unlockable gauntlet — fight every boss back-to-back, FIXED loadout, ONE life, NO tech tree, local best-time board. Unlocks after the FIRST campaign clear (`operationComplete` in ui.js sets `meta.bossRushUnlocked=true`+saves). Pure core in globals.js (MIRROR block, `tests/meta.test.js`): `BOSS_RUSH_POOL` (5×`'boss'`) + `BOSS_RUSH_TOTAL` + `bossRushNext(index)` (boss type for a leg, null past the end) / `bossRushDone(killed,total)` (saturating ≥) / `betterTime(prev,next)` (keep the LOWER time; 0/invalid = no record so first finish wins). Runtime `bossRush`/`bossRushIndex`/`bossRushT0` (globals.js). Flow: `startBossRush()` (ui.js) gates on the unlock, forces `opMode=false`, calls `startGame(jetIdx, false, true)` — the new `rush` param skips `applyMetaPerks` (fixed loadout) + the tutorial; one life is automatic (death → `gameOver`→`endRun`, no time recorded). main.js `handleBossRush(dt)` REPLACES `handleWaves` while `bossRush` (no waves/tech/op-map): spawns the next F4 phased boss via the existing `spawnBoss()` once the arena clears (`spawnBossRushBoss` bumps `bossRushIndex`, banner `bossrush.wave`); `run.boss++` (combat.js boss-kill, unchanged) drives the count; when `bossRushDone(run.boss, TOTAL)` → `bossRushComplete()` records `meta.bossRushBest = betterTime(...)` (+saveMeta) and shows the debrief via `endRun`. META BACK-COMPAT (the key risk): `validMeta` UNTOUCHED (stays lenient); `freshMeta` defaults `bossRushUnlocked:false`+`bossRushBest:0`; `loadMeta` heals legacy saves (`if (typeof meta.bossRushUnlocked !== 'boolean') meta.bossRushUnlocked = false;` + `if (typeof meta.bossRushBest !== 'number') meta.bossRushBest = 0;`) — a save lacking the fields keeps all SP/jets/perks (test-asserted). UI: hangar `#bossRushBtn` ghost button (locked/disabled until unlocked) + `#bossRushNote` (best time / lock note) via `refreshBossRushEntry()` (wired in hangar init, `applyLang`, post-run refresh, and on campaign clear); CSS `.ghostbtn.disabled`/`:disabled`. i18n EN+ZH: `bossrush.title|start|locked|best|sub|wave|cleared`. Tests: extended `tests/meta.test.js` — unlock+best persistence, the CRITICAL legacy-save-keeps-progression assert, validMeta lenience, pure sequence/timing helper asserts + a byte-identity guard against the globals.js boss-rush MIRROR block (+ `freshMeta` mirror updated in `tests/stars.test.js` too, which independently byte-checks it). `npm test` green (56 ok-lines). Pending: in-browser playtest of gauntlet pacing + a richer per-boss pool (all 5 legs currently reuse the same boss spawner — tunable later via `BOSS_RUSH_POOL`).
- Named ace squadrons per sector (2026-06-14, F8): each op-mode sector type spawns one NAMED hostile ace on its first wave — distinct callsign, slightly tuned stats, purple "HOSTILE ACE INBOUND — {name}" banner. Six aces: TALON (FURBALL), BANSHEE (INTERCEPT), REAPER (STRIKE), DAGGER (ESCORT), VIPER (DEFEND), SPECTER (ELITE). No new file: pure pool + selection fns (`HOSTILE_ACES` / `hostileAceFor` / `hostileAceDeltas`) added to `rival.js` (MIRROR block, mirrored byte-identical in `tests/ace-pool.test.js`). `sectorPlan` in `opmap.js` gains `hostileAce` boolean (true for combat sectors, false for DEPOT/FINAL); `nextWave` in `main.js` spawns `spawnHostileAce()` once per sector (guarded by `run.sectorAceSpawned{}` reset in `startGame`). Reuses existing `e.elite`/`e.aceName`/`e.callsign` pathway — `drawEnemy` (ui.js) renders callsign on the HUD marker with no changes needed. Stat deltas bounded: hpMul 1.10–1.18, turnRate 1.45–1.60, speed ×1.05–1.12. i18n: `banner.hostileAceInbound` EN + ZH. `run.sectorAceSpawned` field added to both `run` initializers (globals.js + ui.js `startGame`). All tests green.

- Screen shake + camera kick (2026-06-14, F1 — COMPLETE): brief camera position offset on four events. globals.js adds `camShake` global + `CAMSHAKE_RATE`/`CAMSHAKE_K` tunables + `shakeCam(amt)` (max-wins, no accumulation) + pure `decayShake(v, dt)` (rate ~6/s). ui.js `updateCamera` applies x/y offset scaled by `camShake * CAMSHAKE_K` AFTER the follow logic (so follow is unaffected when camShake==0), then decays `camShake` via `decayShake`. Triggers in combat.js: `shakeCam(0.5)` at missile-hits-player (before `damagePlayer`), `shakeCam(0.5)` inside `damagePlayer` (on the player.hp line), `shakeCam(0.25)` inside `killEnemy` when `byPlayer`. `shakeCam` is a global so a future boss-phase feature can call `shakeCam(0.8)` directly. New `tests/camshake.test.js` (mirrors `decayShake` + shakeCam max-logic byte-identical; asserts monotonic decay to exactly 0, no negatives, max semantics). All tests green.
- Pilot callsign + emblem (2026-06-14, F13 — COMPLETE): PURE COSMETIC — zero gameplay impact. `meta` shape extended with `callsign` (string, ≤8 A–Z0–9), `emblem` (id string, default 'wings'), `patches{}` (SP-purchased emblem ownership). `freshMeta` includes them; `loadMeta` heals legacy saves. `EMBLEMS` table (5 entries: wings=free, skull/star=80 SP each, dragon=bossSlayer ach, ace=acePilot ach). Pure helpers: `sanitizeCallsign(str)` (uppercase, strip non-A-Z0-9, clamp ≤8, empty ok) + `emblemUnlocked(id, m)` (bool; free always, sp-gated via `m.patches`, ach-gated via `m.ach`). Mutators: `buyPatch`/`setEmblem`/`setCallsign`. UI: `#pilotPanel` in hangar (`renderPilotPanel` — callsign `<input>` + emblem button grid; unlocked emblems select on click, SP-gated can be bought, ach-gated shows banner); callsign + active emblem glyph shown in `#go_pilot` on end screen. All strings EN+ZH (`pilot.callsign|emblem|placeholder|buy|locked|needAch`). New tests in `tests/meta.test.js` (sanitizeCallsign charset/clamp/empty; emblemUnlocked all three gate types; freshMeta defaults; legacy-heal via loadMeta; byte-identity extended to include `sanitizeCallsign`+`emblemUnlocked`). All tests green.

- Sound variety (2026-06-14, F12 — COMPLETE): three new synthesized SFX, all gated behind `audio.on` (same guard as `blip`). `engine.js` AudioEngine gains: `lockTone()` — a 300→1600 Hz rising sine sweep (0.32s) that blends into a held 1800 Hz locked tone (0.18s), fires ONCE on the lock-acquire edge; `killSfx()` — a bandpass noise crunch burst (0.18s) layered with a sawtooth 160→30 Hz descending engine fade (0.48s), fires on every player kill; `setEngineJet(jetId, thr, sf)` — wraps `setEngine` with a per-airframe param table (`AudioEngine.jetEngParams`, static method) that shifts `baseAdd` (idle pitch), `ratio` (2nd oscillator ratio), and `lpAdd` (filter cutoff offset) so each of the 13 jets has a distinct engine timbre (twin-whine jets like SU-57/EFT/J-20 use higher ratios; flying-wing/6th-gen jets like J-36/F-47 use lower base freqs and closed filters). Hook sites: `combat.js` lock-acquire edge (`prev < 1 && player.lockProgress >= 1`) now calls `audio.lockTone()` instead of the old `audio.lock() + blip(1850...)`; `killEnemy` byPlayer branch calls `audio.killSfx()` alongside the existing haptic+shakeCam; `main.js animate` loop replaces `audio.setEngine(...)` with `audio.setEngineJet(player.jet && player.jet.id, ...)`. No new files, no new tests (audio is not unit-testable); `npm test` stays green.

- Tutorial / first-run polish (2026-06-14, F5 — COMPLETE): the text-only onboarding now hands off to a guided first wave with contextual stepped prompts gated on the player's own actions. Pure step machine in globals.js — `tutorialNext(step, event)` over `TUTORIAL_STEPS` (`pitch`→`throttle`→`guns`→`missile`→DONE): each step's matching event advances it by one, `skip` jumps straight to DONE, monotonic + terminal (MIRROR block, mirrored byte-identical in new `tests/tutorial.test.js`). Runtime state `tutorial = {active,step,done,prevShots,prevMissiles}` (globals.js). ui.js owns the overlay: `startTutorial`/`renderTutorial`/`advanceTutorial`/`finishTutorial` + `tutStepKey` (touch-aware: `tut.*Touch` vs `tut.*` keyed off `isTouchEnabled`) + `tutArrowTarget`/`placeTutArrow` (points the `#tutArrow` at the THR meter row / Msl counter via `getBoundingClientRect`); cached `el.tut*`; Skip wired once. main.js `tickTutorial()` (called each frame in the `playing` block after `updatePlayer`) detects the current step's action from live state — `|pitchRate|>0.25`, `throttle>0.6`, `run.shots` tick, `lockedTarget` + `run.missiles` tick (counters baselined at start so pre-spent ammo can't auto-pass) — and feeds the event in. Started only for a brand-new player (`!isReturningPlayer && !tutorial.done`) at first launch in `startGame` (ui.js); returning players skip entirely; reuses `skystrike_onboarded` persistence (no new storage key). New overlay `#tutorial` (non-blocking, `pointer-events:none`; only `#tutSkip` interactive) in index.html + styles.css (z-index 55, between HUD and pause/manual). i18n EN+ZH: `tut.title`/`pitch`/`throttle`/`guns`/`missile` (+`*Touch` variants)/`skip`/`done`; `applyLang` localizes the Skip label + retranslates a live hint. New `tests/tutorial.test.js` (ordered advance, no-regress, skip→done, terminal, monotone fuzz + byte-identity MIRROR guard). All tests green. Pending: in-browser playtest of arrow placement across resolutions (headless can't measure layout; arrow is placed once per step, not re-anchored on resize — YAGNI).

- Daily seeded challenge (2026-06-14, F7 — COMPLETE): a calendar-date seed fixes the run's layout/weather/jet so everyone flies the same sortie each day, one attempt/day, score saved locally (no server). Pure core in globals.js (MIRROR block, `tests/daily.test.js`): `makeRng(seed)` (stateful mulberry32 PRNG — same `Math.imul` mixing family as `rollWeather`; same seed → identical float stream) + `dailySeedFor(y,m,d)` (stable uint32 seed per date, distinct dates → distinct seeds; PURE — never calls `new Date()`). Runtime flags `dailyMode`/`dailySeed` (globals.js). CRITICAL: the clock is read ONCE at the ui.js call site (`todayParts()`/`todayKey()`) and y/m/d passed into the pure fns. `startGame(i, daily)` gained an explicit `daily` flag — `dailyMode = !!daily` (normal launch/dblclick/Enter callers pass only `i`, auto-resetting it); when `dailyMode`, `weatherSeed = dailySeed` so the existing per-wave `rollWeather(weatherSeed + wave)` seam already yields deterministic weather (zero main.js change). `startDaily()` (ui.js): reads today's record, blocks if `played`, derives the seed-restricted jet via `makeRng`, forces `opMode=false` (single-life endless), marks the attempt consumed up-front, launches. `endRun` records the daily best (keeps the higher score) when `dailyMode`. Persistence via `store.get/set('skystrike_daily')` = `{date:'YYYY-MM-DD', played:true, best:score}` (JSON; ui.js `loadDaily`/`saveDaily`/`dailyToday`). Hangar entry: `#dailyBtn` ghost button + `#dailyNote` (index.html); `refreshDailyEntry()` locks the button once played and shows today's best/lock note; wired in the hangar init + `applyLang`. All strings EN+ZH (`daily.title|play|done|best|locked` + `daily.sub`). New `tests/daily.test.js` (makeRng determinism/uniformity + dailySeedFor stability & 1344-date no-collision sweep; pure core mirrored byte-identical). All tests green. Pending: in-browser smoke of the hangar entry (headless playwright not installed here — runtime glue verified via a date/store-mocked simulation against the real pure block instead).
- Star objectives per run (2026-06-14, F6 — COMPLETE): 1–3 secondary stars per run from three independent, checkable conditions, banked as a per-jet lifetime BEST. Pure core in `js/meta.js`: `evalStars(run, player)`→0..3 (kill efficiency ≥`STAR_KILL_FRAC`=0.6 of waves·4 / a full no-damage wave via `run.cleanWaves`≥1 / objectives via `run.missions`≥1) + `bestStars(meta, jetId, stars)` (monotonic best into `meta.stars[jetId]`). New per-run state: `run.cleanWaves` (both initializers) + global `noDamageWave` (globals.js) — armed at each `nextWave` (main.js), cleared in `combat.js damagePlayer` on any landed hit, and folded into `run.cleanWaves` at the wave-clear in `main.js handleWaves`. `meta` shape gains `stars{}`; `freshMeta` defaults it and `loadMeta` heals legacy saves missing it (validMeta stays lenient — back-compat, no progression wipe). `endRun` (ui.js) computes stars, calls `bestStars`+`saveMeta`, and renders `#go_stars` (pips ★/☆ + earned/best). HUD: `drawStarObjectives` (ui.js `drawHUD`) shows the three live objective pips top-centre. HTML `#go_stars` in `#gameover`; CSS `.gostars`/`.stars-title`/`.stars-pips`/`.stars-note`. i18n EN+ZH: `stars.title`/`stars.best`/`stars.obj.kills`/`stars.obj.noDamage`/`stars.obj.rescue`; `applyLang` sets `#goLblStars`. New `tests/stars.test.js` (evalStars 0/partial/all-3 boundaries + bounded-int; bestStars per-jet monotonic + save/load persistence; loadMeta heals a stars-less save without wiping progression; mirrors byte-identical) + `meta.test.js` mirrors updated. All tests green. Stars feed a future cosmetic/SP milestone track (record-only for now).
- AWACS support calls (2026-06-14, F10 — COMPLETE): spend in-run RP (`player.tp`) mid-flight on one of three radio calls, each capped per sector. Pure cost/uses core in globals.js (MIRROR block, `tests/awacs.test.js`): `awacsCall(state, costs, max, key)` → `{ok, reason, rp, uses}` deducts RP and bumps the per-call uses counter ONLY when affordable AND under the cap (reasons `unknown`/`empty`/`noRp`/`ok`; pure — never mutates the input). Tables `AWACS_COSTS` (strike 140 / resupply 90 / jam 70 RP) + `AWACS_USES_MAX` (strike 1 / resupply 1 / jam 2 per sector) + `AWACS_JAM_TIME`=8 + runtime `awacsUses`. Effects glue `awacsAction(key)` (combat.js): commits the deduction then `strike`→`killEnemy(nearestNonBossEnemy(), true)` (nearest live non-boss, any direction; bosses immune), `resupply`→refill `bullets`/`flares`/`missiles` to max, `jam`→`player.jammer = max(player.jammer, AWACS_JAM_TIME)` reusing the existing SPECTRA jam pathway (`updateMissiles` blinds every enemy missile to `m.decoyed` while `jammer>0`). Bindings: keys 1/2/3 (`Digit1`/`Digit2`/`Digit3`, added to `GAME_CODES`, switch in main.js keydown) + touch buttons `tb-aws`/`tb-ars`/`tb-ajm` (index.html, bound in controls.js, localized in `applyLang`). Banners on success (`awacs.strike|resupply|jam`) / failure (`awacs.noRp`/`awacs.empty`). `awacsUses` reset per sector in `nextWave` (main.js) + per run in `startGame` (ui.js). All strings EN+ZH (`awacs.*` + `touch.aws|ars|ajm`). New `tests/awacs.test.js` (cost deduction, uses decrement+cap, insufficient-RP rejection, exact-cost boundary, purity; pure core mirrored byte-identical). All tests green. (Originally bound to F10/F11/F12; rebound to 1/2/3 pre-merge since F11/F12 are browser-reserved fullscreen/devtools.) Pending: in-browser playtest of call feel + button placement.
- Mobile performance pass (2026-06-14, F11 — COMPLETE): VISUAL-ONLY graphics-quality tiering for mid-range phones — zero gameplay change. New `gfxQuality` setting ('auto'/'low'/'high', Settings segtog `#gfxQualityTog`, persisted in `skystrike_settings` via the existing save/loadSettings seam). Pure tier resolver `resolveQuality(setting, dpr, isTouch)` in globals.js (MIRROR block) → 'low'/'high': explicit values pass through, 'auto' picks 'low' for touch devices on dpr ≤ 2 (mid-range signature), else 'high'; impure `refreshGfxTier()` is the call site doing the device read (could fold an fps sample later — headless can't measure fps, so the decision stays pure + testable). Three low-tier savings, all reversible: (1) `engine.js applyGfxQuality()` shrinks `sun.shadow.mapSize` 2048→1024 + `shadow.camera.far` 6000→3000 (disposes the old shadow map to force realloc; called in `initThree` after sun setup + on every settings change); (2) `main.js cullDistantEnemies()` hides distant enemy meshes via `e.group.visible` (NEVER despawns — markers/locks/AI keep running; `GFX_CULL_FAR`=9000 ground, `GFX_CULL_JET`=6500 air fodder as a cheap far-LOD; bosses/rivals always drawn; high tier forces everything visible so toggling quality up mid-run restores draw instantly); ticked right after the enemy update loop in `animate`. i18n EN+ZH: `set.gfx`/`set.gfxAuto`/`set.gfxLow`/`set.gfxHigh`; `applyLang` sets `#lblGfx` + the three segbtn labels; `syncControlSettingsUI` marks the active button. New `tests/gfx-quality.test.js` (resolveQuality auto-heuristic + explicit pass-through + always-concrete-tier fuzz + byte-identical MIRROR guard; the guard searches the END marker AFTER START since globals.js holds several mirror blocks sharing the END text). All tests green; `node scripts/shot.mjs` boots + renders all 4 shots with zero page/console errors. Pending: on-device fps A/B (headless can't measure frame rate).
- Scripted set-pieces (2026-06-14, F14 — COMPLETE): specific op-map NODES now trigger an AUTHORED encounter instead of procedural waves (1–2 per campaign). Pure core in `js/opmap.js` (MIRROR block, `tests/setpiece.test.js`): `SETPIECES` data table (samCorridor = strike a fortified SAM site / bomberRun = escort a convoy through SAM lanes / carrier = intercept a carrier's screen; each carries `mission`/`ground`/`bombers`/`convoy` + name/intro/outro i18n keys) + `setpieceFor(type, stage)` (DETERMINISTIC node→id-or-null, keyed on the FIXED `genOpMap` stage coordinates — STRIKE @ stage 1 = samCorridor, ESCORT @ stage 4 = bomberRun — so the same campaign node always plays the same scripted event) + `setpiecePlan(id, base)` (PURE: folds the encounter's mission/ground/bombers/convoy onto a base `sectorPlan`, returns a NEW object, base untouched, tags `setpiece`) + `setpieceOutcome(id, won)` (win→encounter outro key, fail→shared `banner.missionFailedObj`). HOW A NODE IS MARKED + DETERMINISM: marking is the pure `setpieceFor` lookup against the fixed campaign's (sectorType, stageIndex); no RNG, no clock — fully reproducible. Wiring: `main.js nextWave` computes `setpieceFor(opSector, opStage-1)` (the flown sector sits at `opStage-1` since `launchSector` pre-increments `opStage`), and on the sector's FIRST entry (guarded by new `run.setpieceDone{}`, reset in both `run` initializers) folds the plan via `setpiecePlan` BEFORE the existing spawn loop — so the SAM site (`plan.ground`→`queueStrikeSite`) and escort convoy (via `startSectorMission`→`spawnEscortConvoy`) reuse existing entity pathways with zero new meshes. `missions.js` adds runtime flag `setpieceActive` (set in `startSectorMission` from `plan.setpiece`): leads with the encounter's intro banner instead of the generic objective header, and `onMissionResolved` shows the authored outro on win (cleared on resolve + in `startGame`). Resolution rides the EXISTING typed-mission win/fail seam (`onMissionResolved`) — win pays the same RP/score + `run.missions++`, fail→`gameOver`. Procedural sectors are byte-for-byte unaffected (non-authored nodes return null → vanilla `sectorPlan`). i18n EN+ZH: `setpiece.carrier|samCorridor|bomberRun` (+ `.intro`/`.outro` each). New `tests/setpiece.test.js` (deterministic mapping incl. exactly-2-authored-nodes sweep, pure non-mutating fold, win/fail outcome keys + byte-identical MIRROR guard against opmap.js). All tests green (54 ok). Pending: in-browser playtest of the two encounters end-to-end (headless can't fly the sortie).
- Wave-3 cross-feature note (F14 ↔ F15 boss-rush): F14 added the symbol `run.setpieceDone{}` to BOTH `run` initializers (globals.js + ui.js `startGame`) and `setpieceActive` to missions.js; touched `opmap.js` (set-piece core after `sectorPlan`), `main.js nextWave` (set-piece fold block), and i18n EN+ZH. If F15 also edits these same hotspots, merge by KEEPING BOTH additions (the `run` object just gains both new fields; the `nextWave` op-sector block gains both feature blocks; i18n gains both key groups).

- UX polish pass (2026-06-14, branch `feat/ux-polish`): seven player-facing fixes, all surgical (no new files except the temp shot harness, removed). (1) **Turbulence removed** — `WEATHER.fog.turbulence` 0.05→0 (last nonzero value; the mirror + the three fog asserts in `tests/weather.test.js` updated in lockstep) and the `combat.js updatePlayer` turbulence perturbation block deleted (`turbSample`/`weatherT` left as harmless dead code). (2) **Callsign typing fixed** — `main.js` keydown handler returns early when `document.activeElement` is INPUT/TEXTAREA, so `GAME_CODES` no longer get `preventDefault`'d while typing the callsign. (3) **Dev unlock-all** — new `devUnlockAll` global (globals.js) short-circuits `meta.js jetUnlocked` (`return devUnlockAll || …`); `tests/meta.test.js` mirror updated + `let devUnlockAll=false` added so the byte-identity guard and the lock assertions stay green; Settings checkbox `#setDevUnlock` (i18n `set.devUnlock`) wired in ui.js (`saveSettings`/`loadSettings`/`applyLang`, re-renders the hangar card on toggle). (4) **Controls default → EXPERT** — `controlScheme` default 'auto'→'rate' (globals.js) so desktop flies free-stick; `loadSettings` whitelist fallback also 'rate'; relabeled `set.csAuto/csPointer/csClassic` → AUTO/ASSISTED/EXPERT (EN+ZH). (5) **Daily launch fixed** — `startGame` jet-lock guard is now `if (!daily && !jetUnlocked(...))` so a seed-assigned unowned jet still flies. (6) **Barrel-roll polish** — `BARREL_ROLL_DURATION` 0.4→0.65 (test const updated); new `barrelRollDir` global captured at initiation in `combat.js` (from `rollIn`) replaces the neutral-stick right-bias, plus a mid-roll pitch arc (`tb*0.35*sin`) for a real barrel-roll look; keyboard double-tap Q/E already existed (F9) and was left untouched; one-time `tut.barrelRoll` tip banner shown after tutorial completion (EN+ZH). (7) **AWACS costs surfaced** — `#awacsHud` chip row in the HUD `br` panel (ui.js `updateAwacsHud()` ticked from `updateDom`: remaining uses ×N + RP cost per call from `AWACS_USES_MAX`/`awacsUses`/`AWACS_COSTS`); new Flight-Manual AWACS section (`manH_Awacs`/`manP_Awacs`, i18n `manual.awacs`/`manBody.awacs`); chip labels i18n `awacs.chipStrike/chipResupply/chipJam`. (8) **Pilot identity in-flight** — `#pilotTag` (bottom-left, reuses `EMBLEM_GLYPHS` from ui.js) shows callsign+emblem during play (populated in `startGame`, shown/hidden each frame in `updateDom`, only when a callsign is set); Settings callsign hint `#callsignHint` (i18n `pilot.hint`). New CSS `.awacs-hud`/`.awacs-chip`/`.pilot-tag` in styles.css. `npm test` green (all mirrors intact); headless `shot.mjs` boots with zero page/console errors and a callsign'd in-flight capture confirmed the AWACS chips ("STRIKE ×1 140 RP") and pilot tag ("WRAITH") render without layout breakage.

## Keep this file current
This file replaces codebase exploration — stale info costs more tokens than no info (wrong assumptions → wasted edits/re-reads). When you change code, update the relevant line in the same commit:
- New/renamed/moved file, or new script-load-order entry → update Architecture table + load-order chain.
- New cross-file convention/helper (like `clearLocks`, `airSpawnPos`, storage seam) → add to Hard rules or the table.
- Finish/start a roadmap item → update Current state. Remove stale entries rather than letting them accumulate.

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).
