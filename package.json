{
  "name": "skystrike",
  "private": true,
  "version": "0.1.0",
  "description": "SKYSTRIKE // ACE PROTOCOL — arcade jet combat (Three.js)",
  "scripts": {
    "test": "sh -c 'for f in tests/*.test.js; do node \"$f\" || exit 1; done; echo ALL TESTS PASS'",
    "verify": "sh -c 'for f in scripts/verify-*.mjs; do echo \"# $f\"; node \"$f\" || exit 1; done; echo ALL VERIFY PASS'",
    "build:www": "sh scripts/build-www.sh",
    "serve:https": "node scripts/serve-https.mjs"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0",
    "playwright": "^1.60.0",
    "selfsigned": "^5.5.0",
    "three": "^0.159.0"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.0",
    "@capacitor/ios": "^8.4.0"
  }
}
